# UI pass, 2026-09-14: six surfaces, one grammar

> **For agentic workers:** one task per agent, all in the worktree `/Users/karimbaba/Developer/Apps/phosphor-ui` on branch `feat/ui-pass-sept14`. Read this whole file, then the map at `/private/tmp/claude-501/-Users-karimbaba/de3b5226-ce36-4220-b2de-d301058e1e35/scratchpad/map.md` (file:line pointers for everything) and, for tasks 2 and 4, the research at `.../scratchpad/research/research.md`. Steps use `- [ ]` syntax.

**Goal:** Karim's six screenshots, fixed: the trade rail moves under the chart and its price, figures and fills stop looking like balances; the assistant column becomes the simplest possible chat box with a working state that reads as light, not a sweep; Pro's activity reads as transactions, its Limits card becomes a Policy card, and the Money panel tells coins apart and shows a change; Basic keeps its simplicity and gains one moment of pleasure.

**Architecture:** Vanilla JS and CSS, no framework, no bundler. Every surface consumes the shared grammar already committed in `ui/design/components.css` under "The money grammar" (`.tag`, `.stats/.stat`, `.tx`, `.rule`, `.status-line`) and the rolling digits in `ui/core/dom.js setNumber`. Coin brand colours are already in `ui/design/marks.js` (`PhosphorMarks.colourFor`, `paint`, `COLOURS`) and the disc reads `--coin`. Motion is vendored once (task 2) as `ui/vendor/motion-13.3.0.js`, global `Motion`, and exposed through `PhosphorMotion.spring()`; every animation must degrade to CSS when `window.Motion` is missing, because the unit harness has no window.

**Spec:** Karim's words, 2026-09-14, verbatim where it matters:
- Trade: "this trade panel on the side I want to be below the chart, so the whole chart horizontally. also I want the btc price to actually look like a price tag and not a balance. and the same for the info, because it looks almost exactly like the pro and basic tab balances. transactions should look like transactions too."
- Assistant: "it doesnt look user friendly, it has to be maximum simple and [of one piece] with the rest of the ui ... the best possible chat box."
- Pro activity: "I want these to look like actual transactions."
- Limits: "this thing should be policy and not limits, we shouldnt have limits unless specified. we should [have] a policy that is simple and easy to read and understand ... clear to the eyes."
- Money: "I want to see some more color or some differentiator between tokens so it is clear when something happens. and the scanning thing that the agent does when actually doing something looks super cheap, find a more quality component and animation. maybe use motion.dev and be creative. make sure everything matches the other color mode as well."
- Basic: "I love this simplicity but it brings out no dopamine in me, difficult to admire. simplicity is key here but we still want the client to be happy."

## Global constraints

- Two colourways, both must look finished: `green-on-black` and `black-on-white`. Screenshot both for every surface you touch, with the rig: `/private/tmp/claude-501/-Users-karimbaba/de3b5226-ce36-4220-b2de-d301058e1e35/scratchpad/shot.sh <view> <green|white> <out.png>` (views: basic, pro, trade, assistant, assistant-working, plus trade-full and pro-full once the rig agent adds them). Look at every PNG you produce with the Read tool before you call anything done.
- Colour means state. The only non-state colour is the coin brand colour, and it lands only on the 20 px mark and an allocation bar, never on a number.
- Type: Geist for words, Geist Mono with tnum for any number that can change. Sentence case everywhere. No uppercase labels, no tracked eyebrows, no middle dots, no arrows glued to link text, no em or en dashes in code, comments or copy.
- Copy is the app's voice: plain English a non-expert reads in one look, active, the same word for the same thing.
- Motion: ease-out `cubic-bezier(0.23,1,0.32,1)` for enters, under 300 ms for UI, springs through `PhosphorMotion.spring()` once task 2 lands (guard: fall back to `var(--ease-out)`). Reduced motion keeps opacity changes and drops movement. Never animate width, height, padding or margin.
- Tokens: tests/unit/theme-slots.test.ts slices the `:root {` block and the `:root[data-profile="black-on-white"] {` block of tokens.css to their FIRST `}` and compares nine slots with src/view/theme.ts. New tokens with new names are fine; never put a `}` inside those two blocks.
- CSS ownership: edit only the sections the map's section 8 assigns to your surface, plus new sections you append at the end of that file. Shared classes (panel*, row*, facts/fact, chip/dot, btn*, tick, empty, meter, scrolls, the utilities, the type tones, and the new money grammar) are frozen: extend with a modifier in your own section, never edit the shared block. Use Edit, never Write, on ui/design/*.css and ui/index.html: three other agents are editing other sections of the same files right now.
- Tests: `node --test tests/unit/*-ui.test.ts tests/unit/trade-*.test.ts tests/unit/theme-*.test.ts tests/unit/basic-view.test.ts` must pass (460 now) and `npm run typecheck` must be clean before every commit. A test that asserts markup you changed on purpose is updated to the new contract, never deleted, and the commit says why.
- Commits: small, one coherent change each, message in the repo's style (a plain sentence saying what the window now does and why, no prefix, no AI attribution). Commit on the branch you are on; never merge, never push, never touch the main checkout at /Users/karimbaba/Developer/Apps/phosphor.
- The demo backend and the rig are the only things you run. Never run the app against config.local.json or real keys.

---

### Task 1: Trade, the deck under the chart

**Files:**
- Modify: `ui/screens/trade.js` (build 103-166, renderStatus 631-693, renderDone 925-988, comments at the head)
- Modify: `ui/split.js` (the `trade` entry, 63-82)
- Modify: `ui/design/layout.css` trade section (618-838) and the resizer (840-855)
- Modify: `ui/design/screens.css` trade rail section (1270-1490)
- Test: `tests/unit/trade-fills-ui.test.ts`, `tests/unit/split-ui.test.ts`, `tests/unit/trade-rail*.test.ts`

**Interfaces:**
- Consumes: `.tag`, `.tag-delta`, `.stats/.stat`, `.tx[data-dense]` from components.css; `dom.setNumber` (rolls digits); `PhosphorMarks.disc(symbol)` for the coin mark on a fill.
- Produces: `.trade-wrap` is a column now: `.trade-main` (bar + chart) above, a horizontal `.split-h` handle, then `.trade-rail` renamed in spirit to a deck but keeping the class name `.trade-rail` (tests and the spotlight read it). The split entry becomes `axis: 'y', sign: -1, min: 168, pane: '.trade-rail', host: '.trade-wrap', prop: '--deck', give: '.trade-main', giveMin: 320`.

**Layout.** `.trade-wrap { display: grid; grid-template-rows: minmax(0, 1fr) 6px var(--deck, 236px); }`. The deck is a three column grid: `grid-template-columns: 240px minmax(0, 1fr) minmax(0, 1.15fr)`, hairline rules between columns, each column scrolling inside itself with the `cuts` fade the rail already paints. Column 1 is Status, column 2 stacks Open above Waiting (each as tall as its rows, Waiting takes the rest and scrolls), column 3 is Done. Zone headings stay `h2` in the same order Open, Waiting, Done (trade-fills-ui asserts the order) and gain a count: "Open" becomes "Open" plus a mono `<span class="trade-zone-count">2</span>` when the count is above zero. Under a container width of 760 px the three columns stack and the deck scrolls as one column; under 860 the whole wrap goes back to a scrolling column exactly as the current `@container world (max-width: 859px)` block does today, adapted to rows.

**The price tag.** Replace `.trade-mark` with: the coin as a 12 px word above, then a `.tag` holding the price (digits rolled through `dom.setNumber`) and a `.tag-delta` with the move since the last tick, signed, one decimal, in percent, or the 24 h change if the payload carries `prevDayPx` or `dayChangePct` on `markets[]` (check src/trade/view.ts; if neither exists, the delta is since the window opened, and it says so in a `title`). `data-dir` on the tag and the delta follows the direction of the last change; a change sets `data-flash="true"` for 300 ms then clears it (setTimeout, tracked so a second tick restarts it). Keep the `.trade-mark-coin` class on the coin word (tests read it) and put the tag beside it, so the surface still carries the same text.

**The figures.** Replace `accountFacts` with a `.stats` row of `.stat`s: Free, At risk, and Max loss only when above zero. Same text, same numbers, different shape: label above value. Under them, when At risk is above zero, a single `.rule-meter` style 2 px line showing at-risk as a share of free plus at-risk, no label. Keep the venue-down and unknown paths exactly as they read today.

**Done as transactions.** Each done row becomes `.tx[data-dense="true"]` (keep the class `done-row` on it as well: `class="tx done-row"`, the spotlight and the tests read `done-row`). Children in this order: the coin mark (`PhosphorMarks.disc(fill.coin)`, or a `.tx-mark` with a small check glyph for an ended plan), `.tx-title` ("Sold ETH 0.0592" as today, ended plans keep their sentence), `.tx-amount` (the fill's notional `sizeCoin * px` as usd, `data-dir="in"` for a sell and `"out"` for a buy; an ended plan keeps its id in `.dimmer`), and a `.tx-sub` that is hidden in dense mode but carries "at $2,518.90, 18:53" for the title attribute. Check trade-fills-ui first: it asserts exact text on `done-row` children and `.meta.mono` for the clock, so keep `<span class="meta mono">18:53</span>` as the FIRST child if a test reads children[0] as the clock, and put the mark before the title instead. Read the test before deciding; do not guess.

**Open and Waiting** keep their markup (tests) and only take the deck's column widths. The Close and Cancel buttons stay.

- [ ] Read the map sections 1, 7, 8 and `tests/unit/trade-fills-ui.test.ts` (all 50 titles) before touching anything.
- [ ] Run the trade tests and split tests, confirm 100 percent green as the baseline.
- [ ] Rewrite `build()` and the layout CSS for the deck; run the split tests; update `split-ui.test.ts` for the new axis and floors (the test names the geometry; the new numbers are 168 and 320, axis y).
- [ ] Shoot `trade` and `trade-full` in both profiles; look at them; fix what is wrong.
- [ ] Rewrite `renderStatus` (tag + stats), run tests, shoot, look.
- [ ] Rewrite `renderDone` (dense tx rows), run tests, shoot, look.
- [ ] Update the file's head comment (it describes a rail on the right) so it describes the deck.
- [ ] Commit after each of the three renders. Final: all trade and split tests green, typecheck clean, four PNGs viewed.

**Deliverable back to the lead (at most 15 lines):** commits (hashes), test counts before and after, the four PNG paths, and anything you could not do.

---

### Task 2: The assistant column and the working state

**Files:**
- Create: `ui/vendor/motion-13.3.0.js` (copy of `motion@13.3.0/dist/motion.js`, fetched from `https://cdn.jsdelivr.net/npm/motion@13.3.0/dist/motion.js`, verify the first line names motion and the license header is MIT, check the byte count is close to 146,960)
- Modify: `ui/index.html` (one script tag before `ui/design/motion.js`; use Edit, it is shared)
- Modify: `ui/design/motion.js` (add `spring()`: returns the easing part of `Motion.spring(0.4, 0)` cached, or `'cubic-bezier(0.23, 1, 0.32, 1)'` when `window.Motion` is missing; also set `--ease-spring` and `--dur-spring` on `document.documentElement` at boot from the same call, guarded)
- Modify: `ui/screens/agent.js` (the header, the meta line, the empty state, the composer, the live step row; map section 2)
- Modify: `ui/beam/beam.js` paint/hold/release (203-244): the `.surface-scan` sweep goes; the hold now sets `data-live="true"` on the column's status line instead
- Modify: `ui/design/components.css` 393-439 (the scan keyframes, delete) and the conversation sections of `layout.css` and `screens.css` named in map section 8
- Test: `tests/unit/agent-transcript-ui.test.ts`, `tests/unit/agent-panel-ui.test.ts`, `tests/unit/beam-ui.test.ts`, `tests/unit/trace-ui.test.ts`

**Interfaces:**
- Consumes: `.status-line` grammar; `PhosphorMotion.reduced()`.
- Produces: `PhosphorMotion.spring()` (string), used by `dom.setNumber` already (guarded).

**Direction.** The column is the assistant's seat, not a chat widget. Read research.md sections 1 and 2 first; then:
1. Header: "Assistant" (not "Your assistant") with the seat light at its left: the `.status-line` dot, still when idle, breathing while a tool call is held. The Off/On pill becomes the verb of the status line: "Off", "Ready", "Working" as the `.status-verb`, so the header reads "• Ready". Under it one meta line as today ("phosphor-mcp, can ask" and the call count in mono). Keep every `data-state` and `data-seat` attribute the tests read; change classes only where the map says no test reads them.
2. Empty state: the hex glyph in the beam colour at 40 percent opacity (not grey), the sentence "Nobody is at the wheel." at 16 px, one line under it, then the primary "Start your assistant" and a text button "Connect your own" (a `.btn-quiet`), and under them three suggestion rows the person can click to prefill the composer: "What do I hold?", "Is anything waiting on me?", "Find a trade on BTC". Suggestions are `button.suggest` rows, hairline ruled, 13 px, appearing with the blur reveal from the research (opacity + translateY(6px) + blur(4px) to clear over 500 ms, 60 ms stagger, reduced motion: opacity only). Keep the copy "Nobody is at the wheel" exactly (a test reads it).
3. Composer: one field on the column's own ground with a hairline top rule, no box around the box. The textarea grows to six lines (rows=1, auto-height on input), Enter sends, Shift+Enter is a newline. The Send control is inside the field on the right: a 28 px round button with an up arrow glyph (inline SVG), quiet grey until there is text, then the ink fill; disabled while the assistant is off. The hint line "Start your assistant to talk to it." goes: the placeholder says it instead when the assistant is off ("Start your assistant to talk to it.") and "Tell your assistant what to do." when it is on. Focus ring: `box-shadow: 0 0 0 2px var(--ink-edge)` on the field.
4. Working state: delete the sweep. While a tool call is held: the header status line is live (dot breathes, verb shimmers, and the verb is the step's own words, e.g. "Reading the chart"), the elapsed counter counts seconds in mono, and the live step row in the transcript keeps its text but drops any spinner in favour of the same 6 px dot. On release the dot settles with one WAAPI ring (scale 1 to 1.6, opacity 0.6 to 0 over 500 ms, spring easing) and the verb returns to "Ready". The wordmark glyph `data-state=working`, the field state and the chip `.dot-live` stay as they are (they are the other layers, map section 2) unless one of them visibly fights the new line, in which case quiet it and say so.
5. Both colourways: on white, the beam colour is #111 so the dot and the shimmer must still read; check the PNG.

- [ ] Read map sections 2, 7, 8, research sections 1 and 2, and the four test files' titles.
- [ ] Vendor motion, add the script tag, add `spring()`; open the app once with the rig's `--probe` to confirm `window.Motion` exists and `PhosphorMotion.spring()` returns a `linear(` string. Commit.
- [ ] Composer and header; run the agent tests; shoot `assistant` in both profiles; look. Commit.
- [ ] Empty state with suggestions; tests; shoot; look. Commit.
- [ ] Working state; run beam-ui and trace-ui (they assert `.surface-scan` and `--scan-h`: update them to the status line contract, keeping their intent, which is that the beam paints while a call is held and stops on release); shoot `assistant-working` in both profiles; look. Commit.

**Deliverable back to the lead (at most 15 lines):** commits, tests before and after, PNG paths (assistant and assistant-working, both profiles), the exact `PhosphorMotion.spring()` output string, anything not done.

---

### Task 3: Pro, transactions, policy, money

**Files:**
- Modify: `ui/screens/receipt.js` (row markup 171-204)
- Modify: `ui/screens/pro.js` (the Activity panel; the Limits panel 737-897; the Money panel rows and bar 380-520)
- Modify: `ui/design/screens.css` and `layout.css` pro sections (map section 8)
- Test: `tests/unit/basic-view.test.ts` (Activity rows also render on Basic through receipt.js; keep the source literals it checks), plus a new `tests/unit/pro-policy-ui.test.ts` for the policy card

**Interfaces:**
- Consumes: `.tx` (full, not dense), `.rule`, `.rule-meter`, `PhosphorMarks.disc/colourFor`, `dom.setNumber`.
- Produces: nothing other tasks use.

**Activity as transactions.** Each receipt row becomes `button.tx.receipt-row` (keep `receipt-row`: receipt.js opens the receipt from it). Children: the coin mark of the coin that LEFT (the "of your Solana (SOL)" coin, parse the symbol from the receipt's own fields, `receipts.ts:28-47`, never from the sentence), `.tx-title` = the server's sentence as it is, `.tx-when` = `dom.ago(...)` as today, `.tx-amount` = the amount that left, signed: "-0.049 SOL" with `data-dir="out"`, and `.tx-sub` = the fee as "$0.02 fee" (singular when it is one fee, the server's own wording otherwise). If the receipt carries what arrived, the sub line reads "+0.0018 ETH, $0.02 fee". The Activity head keeps its title and its "19 receipts, $0.42 in fees" line; the foot "Fees in this window" stays.

**Policy, not limits.** Rename the panel title to "Policy" and its data-surface stays whatever it is (Basic's test keeps the 'rules' literal, check). The sub line becomes a count and a state: "4 rules, nothing spent today" or "4 rules, $120 spent today". The "Limit per day" row and its wide meter go. Rules render as `.rule` rows, in this order, each from the server sentence the map names (src/policy/render.ts through `sentences`, filtered in pro.js 767-769):
1. Ask: "Asks you before anything above $100." with a hand glyph.
2. Refuse at once: "Refuses any single transaction above $10,000." with a wall glyph.
3. Refuse per day: "Refuses more than $25,000 in any 24 hours." with the same wall glyph, and under its sentence the `.rule-meter` with `--used` as the share spent today and the `.rule-figure` "$0 used" on the right. Draw the meter only when a daily cap exists.
4. Gas: "Keeps gas back on each chain." with a coin glyph and, as the `.meta` line under it, "Base $1, Arbitrum $1, Solana $0.50" (the parsed values from 781-800, joined by commas, no ellipsis).
5. Destinations: "Pays only 11 wallets of yours, 1Click and NEAR Intents." with a door glyph; the chevron and the expansion behaviour stay as today (it opens the list).
A rule that is not set does not render, and the count in the sub line counts only rendered rules: that is what "no limits unless specified" means here. If the policy carries no daily cap, rule 3 is absent and nothing says "$0 of $0". The foot becomes one line: "Ask your assistant to change a rule. Every change waits for your click." The glyphs are four inline SVG paths of your own in currentColor on a 0 0 16 16 box, 1.5 px stroke, kept in a small `GLYPHS` table at the top of pro.js.

**Money.** The panel's bar under the total becomes an allocation bar: one segment per coin, width by usd share, each segment in `PhosphorMarks.colourFor(symbol)` (a coin without a colour takes `var(--text-3)`), 1 px gaps, 4 px tall, rounded ends. The rows keep their markup; the coloured discs already land from marks.js. The change signal: when a row's usd or qty changes between two frames, the row gets `data-changed="true"` for 1.2 s: the usd figure rolls (dom.setNumber does it), the row's ground tints to `color-mix(in srgb, var(--coin) 8%, transparent)` and back, and a delta appears beside the usd as `+$0.42` / `-$0.42` in up/down, fading after the same 1.2 s. Wire `--coin` onto the row from `colourFor` so the CSS can read it.

- [ ] Read map sections 3, 7, 8 and the receipt shape in `src/http/receipts.ts:28-47`, and `src/policy/render.ts:30-66` for the exact sentences.
- [ ] Activity rows; shoot `pro-full` in both profiles (or `pro` if the rig has no receipts yet: then also verify with the unit harness that a receipt renders the four children); look. Commit.
- [ ] Policy card with a new unit test that feeds a policy with and without a daily cap and asserts the rendered rule count and the absence of the meter; shoot; look. Commit.
- [ ] Money bar and the change flash; shoot; look. Commit.

**Deliverable back to the lead (at most 15 lines):** commits, tests before and after, PNG paths (pro in both profiles), anything not done.

---

### Task 4: Basic, one moment of pleasure

**Files:**
- Modify: `ui/screens/basic.js` (hero 40-75 and 226-239; the holdings 280-320)
- Modify: `ui/design/layout.css` and `screens.css` basic sections (map section 8)
- Modify: `ui/design/pattern.js` only if the field needs a "changed" pulse and one already exists to reuse (map section 2 names STATES 58-63); never add a new WebGL program
- Test: `tests/unit/basic-view.test.ts`, `tests/unit/basic-coins.test.ts`

**Interfaces:**
- Consumes: `dom.setNumber` (already rolls), `PhosphorMarks`, `PhosphorMotion.spring()` (guarded).

**Direction.** Keep everything Karim loves: the quiet big number, the one sentence under it, the strip, the one panel. Add exactly three things:
1. The balance rolls its digits when it changes (dom.setNumber already does this; make sure the hero uses `setNumber`, not `setText`, and that the `.balance` element keeps the `tick` class). Under the balance, a delta line in the direction colour, mono 13 px: "+$0.42 today" when the payload carries a day change (look for a history or worth field in the basic frame, src/view/basic.ts; if none exists, compute the change since the window opened from the first total seen this session and word it "+$0.42 since you opened"; if the change is zero, show nothing). Never show a fake number.
2. The strip (policy sentence) gets the same hand glyph the Policy card uses, 14 px, at its left, so the rule reads as a rule rather than a stray sentence; keep its text.
3. The "What you hold" rows: the discs already carry brand colour. Add the allocation bar (same shape as task 3's, 4 px, one segment per coin by usd share, coin colours) between the hero and the strip, full column width, with no label. It is the one coloured thing on the page and it is information (what the money is made of). A row whose value changed gets the same 1.2 s tint and delta as task 3 (copy the CSS shape into the basic section; do not edit task 3's section).
Then stop. Do not add cards, gradients, illustrations or a second animation. Check the field behind the column still reads on both colourways.

- [ ] Read map sections 4, 7, 8 and the two tests' titles.
- [ ] Hero delta and roll; tests; shoot `basic` in both profiles; look. Commit.
- [ ] Allocation bar and the strip glyph; tests; shoot; look. Commit.
- [ ] Change tint on rows; tests; shoot; look. Commit.

**Deliverable back to the lead (at most 15 lines):** commits, tests before and after, PNG paths (basic in both profiles), anything not done.

---

### Task 5: Review (the lead, or a fresh reviewer at the strongest model)

Shoot every view in both profiles after all four tasks land, view them beside the before set at `.../scratchpad/before/`, and write the emil-design-eng Before/After/Why table for what still reads wrong. Fix inline. Run the full suite (`npm test`), typecheck, then merge `main` into the branch, resolve, run again, fast-forward `main`, push.

---

### Task 6: Two functional bugs (Karim, 2026-09-14, mid-pass)

Karim: "when a trade happens, basic mode doesnt update the balance, and also when swapping by hand from pro to trade and stuff, it cant tell what screen it is on right now, so make sure the agent has a clear thing to see what exactly screen its on". His screenshot shows Basic's hero reading "checking your new balance" in the balance type, and it stays that way.

**Files:** whatever the bugs are in, found by reading, not guessing: start at `src/view/basic.ts` (the hero state and the "checking your new balance" sentence), `src/http/state.ts` (the frame), the trade fill path (`src/trade/*`, where a fill lands and what it invalidates), `ui/screens/shell.js:63-71` (view switching), `src/view/view.ts` (`set_view_mode`, the state index that `start` returns), `src/mcp.ts` (what every tool result carries). Modify only what the fix needs. Do not touch `ui/screens/basic.js` render functions beyond the state that carries the hero sentence (task 4 owns the hero markup); if the fix must land in basic.js, keep it to the data path and say exactly which lines.

**Bug 1, the balance after a trade.** Reproduce in the demo backend first (the rig's `--probe` shows how to boot and seed; a fill can be seeded through the agent door the way the rig seeds `trade-full`). Find why the hero shows the waiting sentence and never the new total: a refresh that is never triggered after a fill, a total that excludes the trading account, or a "checking" state with no exit. Fix the cause, add a unit test that plays a fill and asserts the basic frame's total and state line, and make sure the waiting sentence, when it is legitimately shown, is set in the state line under the number, never in the number's own slot.

**Bug 2, which screen the agent is on.** The human switches views by clicking tabs; the server must know. Make every human view switch write the view to the server (a human door such as `POST /api/view {token, view}` if none exists, or the existing one shell.js should already be calling; check first), and make the current view unmistakable to the agent: `start` and `switch` return `screen: { view: 'trade', since: <iso> , by: 'human' | 'agent' }`, and every tool result's digest line carries the view name (find where the digest is composed in src/mcp.ts and add "screen: trade" to it). Add a unit test: a human POST switches the view, the next `start` reports it with `by: 'human'`. Update `docs/` where the state index is documented, and the tool description text if it names the field.

- [ ] Reproduce bug 1 in demo mode and write down the cause in one line before editing.
- [ ] Fix, test, commit.
- [ ] Bug 2: find the doors, fix, test, commit.
- [ ] `npm test` and `npm run typecheck` green.

**Deliverable back to the lead (at most 15 lines):** the cause of each bug in one line each, the commits, the tests added, and any part of the fix that had to touch a file another task owns.
