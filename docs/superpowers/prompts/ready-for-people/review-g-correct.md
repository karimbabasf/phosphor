# Review G, correctness. Tip 4240cd0, worktree phosphor-rfp-g-launch, backend on 4213 (quit, scratch dir deleted)

1. Verdict: ACCEPT. Every claim I could reproduce held; the one red test is the changelog heading the lead's 0.8.0 bump owes, and the three native message boxes in main.rs are real and filed, not hidden.

2. Criteria
- 13.1 PASS. `curl -sL .../v0.7.0/SHA256SUMS` -> `211b95fc...f48c Phosphor-macOS-arm64.dmg`; `curl -sL phosphor.karimbabasf.com/download/mac | shasum -a 256` -> `211b95fc...f48c`, 55373867 bytes, content-type application/x-apple-diskimage; `gh release view v0.7.0` -> 5 assets, published 2026-09-19T22:57:56Z; `git cat-file -t v0.7.0` -> tag. docs/getting-started.md:24 "## The Gatekeeper warning", :26 "not notarized by Apple yet"; known-limits.md:46.
- 13.2 PASS. Booted `node src/main.ts` with PHOSPHOR_APP_DATA=1, PHOSPHOR_MODE=live, an absent `.../support/com.karimbabasf.phosphor/state`, scratch HOME: state dir made with policy.json, audit.jsonl, agent.secret (0600), audit.tip.json; log says `wallet no_wallet at $HOME/.phosphor/phosphor-rfp-g-launch/keys.enc.json`; `$HOME/.phosphor` not created. Matches reference.md:381-395 and config.ts:240-250 (`slug = basename(baseDir)`, so the .app gets `~/.phosphor/phosphor/`), config.ts:343 mkdirSync.
- 13.3 PASS. `npm test` -> 3076 tests, 3075 pass, 1 fail: `tests/unit/docs.test.ts:55` "changelog starts with ## 0.8.0 but package.json is 0.7.0". That is the only red on the tip (demo-rail green after the merge). Five claims vs code: policy.md:14-17 ($10,000/$25,000/$100/$500) = the fresh policy.json my boot wrote; money.md:48 "20 percent below the quote" = slippage.ts:14 MAX_SLIPPAGE_BPS 2000; money.md:49-52 (a priced swap under the threshold runs, a quote-valued one always waits) = draft.ts:171-195; reference.md:77 `proposals` limit 50, lead only = wallet.ts:81, context.ts:125; reference.md:296 `POST /api/terms/accept` = router.ts:149; "46 tools" (tools.md:3, architecture.md:14) = EXPECTED_TOOLS.length 46. `npm run typecheck` exit 0.
- 13.4 PASS. main.rs:56-62 HELP_LINKS docs, Report a Problem, Security, Terms, Privacy; :207-217 Copy Log for a Report inserted after Report. `/`, `/terms/`, `/privacy/`, `/security/`, `/docs/` all 200. src/terms.ts untouched, TERMS_VERSION 2026-09-17. `cargo test` -> 24 passed, 0 failed, the three new names green.
- 13.5 PASS (ready, not run, as claimed). tauri.conf.json:50-53 endpoint + pubkey; `.../releases/latest/download/latest.json` -> version 0.7.0, versioned url, 408-char signature; `.../v0.6.0/latest.json` -> 0.6.0. No in-app update run (needs a release above 0.7.0; checklist §6).
- 13.6 PASS for the dead-backend half: main.rs:668-671 opens the window only after `phosphor_is_listening(port, Some(&nonce))`, :503-527 respawns once; tests boot-nonce.test.ts:90,109, window-token.test.ts:137, code-signing.test.ts:92 exist under the quoted names. NSAlert half stays OPEN on the branch: `grep -rn '\.dialog()\|MessageDialog' src-tauri/src` -> main.rs:348, :353 (Copy MCP Config arms), :366 (`fail`), :377 (`notify`), all listed in report §5.2 with diffs. Health on boot: see finding 4.
- 13.7 PASS on the letter, FAIL on the shape test. Appended one audit line holding this boot's seat, the window token, a 0x64hex key, a bare 64hex, `secp256k1:`+88 b58, `ed25519:`+88 b58, an `sk-ant-` key and a JWT, under neutral keys and under `privateKey`/`secret`/`apiKey`/`authorization`. GET /api/log and log_tail (200 both): seat redacted, token redacted, `privateKey`/`secret` fields `[redacted]`; every shape LEAKS on both readers, and `apiKey`/`authorization`/`headers.Authorization` leak. Documented as design in log-tail.ts:19-23 and report §4; no tool takes key material (tool-surface, mcp.ts:303 logs args minus seat/token). Raw shell request `GET /api/log?limit=200` with only a Host header -> `HTTP/1.1 200 OK`, JSON array (LOG_LIMIT_MAX 2000). `gh api .../private-vulnerability-reporting` -> enabled true; .github/ISSUE_TEMPLATE has bug_report.yml (ids version, os, log), config.yml (Security first), feature_request.yml; PR template present. `report_url` test green.
- 13.8 PASS. release-checklist.md: sections 0-7 carry every 0.6.0 step (push main alone, annotated tag alone, five assets, SHA256SUMS on the page, site bytes equal, lsregister), each `ready` or a named blocker (bump, Karim's finger, site). `git tag` -> 12 tags, newest v0.7.0; `git log origin/main..HEAD` 38 local commits; origin/main 6347d9d = the last CI run's headSha (success), release.yml v0.7.0 success.
- 13.9 PASS. known-limits.md headings :7 alpha with real money, :17 key in memory, :31 seat secret and a local program, :46 not notarized; `grep -ci '3452114377\|ticket'` -> 0. No em or en dashes in any G doc.
- 4.4 PASS. injection.test.ts:366-374 `deepEqual(tools.map(name).sort(), [...EXPECTED_TOOLS_SORTED])`; mcp.ts:319 `version: VERSION`.
- 4.5 PASS. injection.test.ts:348 asserts client `phosphor-injection` on the roster and never `phosphor-mcp`; agents.test.ts:44 seats `codex` beside `claude-code`.
- Sweep PASS on the branch: first run `SWEEP PASS: 6 checks`, 811 tracked files, 5935 blobs. Planted a random 64hex: untracked file not seen (check 1 is `git ls-files`, by design); `git add -f` -> `FAIL tracked content ... planted.ts:1 pattern=hex64`; unstaged and deleted, `git status` clean.
- Eval count PASS. eval.ts:5,47 no longer say 28; `npm run eval` -> `29 scenarios: 29 pass, 0 fail, 0 xfail, 0 error`, exit 0.

3. Frozen rules touched
- 4: log-tail.ts asks the holders (`agents.recognises`, `tokenMatches` auth.ts:149-154, hash compare) and copies nothing; sweep.ts:113-283 excuses only published constants by exact value. Holds.
- 10: sweep.test.ts builds its DIGEST at runtime; keys-path.test.ts pins config.ts:240-250 as it stood; eval.ts diff is comments only. Holds.
- 11: no push, tag, release or site change (13.8 output; live /docs/ still says "version 0.6.0", which proves the site was not touched). Holds.
- 1, 2, 3, 5, 9: `git diff main...rfp/g-launch --stat -- src/proposals` empty; view.ts untouched.

4. Findings
1. Sweep excuse hole: sweep.ts:342 skips a whole line before scanning when `publicFormat` matches, so `checksum = "<64hex>"` in ANY file named Cargo.lock passes. Reproduced: a staged scratch `Cargo.lock` with a random key on that line -> no finding. Needs deliberate placement; say so in the file comment or scan the value anyway and excuse only crates.io-registered checksums.
2. Sweep now FAILS in this worktree on the shared .git: `git history` finds hex64 in evidence-b/demo-kill9-4202.txt:5-9 and demo-withdraw-walk-4202.txt:26-27 (blobs f2141bf3, 0dc44309, on rfp/b-hyperliquid, committed after G's last run). Merge-time: node B excuses or changes them, or the merged sweep is red.
3. Second wall misses shapes that can never be a tx hash: `ed25519:`/`secp256k1:` + 87-88 base58 (the sweep's own base58-64byte rule), `sk-`/JWT, and field names `apiKey`, `authorization`, `Authorization`. One regex and four names in log-tail.ts. `diagnose` (wallet.ts:347-351) formats audit lines with `withoutAddresses` only, report §5.5.
4. shell.js:230 starts the health poll only on offline/reconnecting/stale; a boot whose stream connects never calls /api/health. Literal 13.6 ("checked by the window on boot") is not met; the window still cannot sit on a dead backend (stream or poll). Node E's file.
5. Live site: /docs/ says "version 0.6.0", changelog page lists 0.6.0 first, /updates/latest.json 404. Outside the job, Karim's push; report §5.7 has it.

5. Claims not reproduced
- report §2 "sweep PASS, 810 files, 5840 blobs": PASS reproduced at 811/5935 on my first run; later runs FAIL on 5945 blobs from node B's new commits (finding 2), not G's content.
- report §3 13.7 "Copy Log puts 200 lines on the clipboard": not clicked in a bundled app; the exact raw request it sends was reproduced against my backend (200 OK, JSON array).
- report §3 13.1 "the app inside is ad hoc signed": DMG not mounted here; sha and site bytes reproduced only.
- report §2 test counts were pre-merge (demo-rail red); on tip 4240cd0 the single red is docs.test.ts:55, as the brief expects.
