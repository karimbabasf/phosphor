# Review E (correctness): rfp/e-craft at ed0f0e4

1. Verdict: REJECT (narrow). The CSS floor, the dock cap and the tests are real and reproduced, no frozen rule is touched, but B6 fails on the send card at the reviewed tip (No/Approve 135 px under the fold at 400x800, 109 px at the default 1180x780 window), and the report passes B1, B2 and B7 by scoping the check narrower than the criterion. The builder's working tree already holds an uncommitted send-card fix (layout.css, sendcard.css, mtime 20:46); commit it, re-measure, and the verdict flips.

2. Criteria
- B1 FAIL (one family). `node scripts/button-inventory.ts` (INVENTORY_OUT in scratch): "108 measurements, 0 with a problem", every .btn at 36/30/44, every non-.btn at 30 or more, no wrap, no clip. But scripts/button-inventory.ts:273 exempts btn-quiet from the label+24 check; `.btn.btn-quiet` (Skip firstrun.js:347, Copy receipt.js:623, Your rules vault.js:201, Done vault.js:777) measures box 49.5 with a 31.5 label = 18 px of room, under 24. The exemption is not in the report's Decisions.
- B2 FAIL (non-.btn half). Sheet at 860 and 400 (read, s860-0/1.png): every .btn family shows five states visibly different, pending shows one face (Approving, Freezing, Starting, Saving). But `grep -rhoE "[^{}]*:active" ui/design/*.css | sort -u` gives 11 selectors; 20 of the 54 inventoried families have no :active rule (dock-close, dock-next, dock-report-toggle, steps-fold, pane-hide/show, receipt-close, sendcard-info, checks-toggle, activity-link, netpick-link, trade-tab, fold-head, jump-latest, lock-eye, net-row, netpick-back, netsel, tx receipt-row): active = rest. The report scopes "five states" to .btn families (button-inventory.test.ts) without saying so.
- B3 PASS. reset.css:91 `[hidden] { display: none !important; }`; design-buttons-ui test 1 walks every sheet for a weighted display outside a [hidden] guard (green); `.btn:disabled:not([data-pending="true"]) { opacity: 0.5 }` components.css:74, `button:disabled { cursor: default }` reset.css:57; sheet disabled cells visibly dimmer; `[data-pending="true"] { pointer-events: none }`.
- B4 PASS with a caveat. icons.js and cards.js glyphs are both 24-grid, 1.5 px stroke (icons.js:156, cards.js:171), so one visual family. Literal mix exists in a pressable the scan never sees: `.tcard-head` role=button (cards.js:247) carries an icons.js head icon (cards.js:1136, 1197) plus the cards.js chevron (cards.js:266). checks.js:58 chevron is 1.6 px, not the 1.5 the report says.
- B5 PASS (read only). reset.css:76 `:focus-visible { outline: 2px solid var(--ink) }`; Enter/Space native on <button>, cards.js:293 for the role=button head; Escape closes the Layout menu (shell.js:458), Layers (trade.js:554), receipt is a native <dialog>. Dock read card: no Escape (decision.js:8 by design), filed as request 1, not faked (no JS in the diff). Netpick sheet also has no Escape (netpick.js, deposit.js: no keydown for it), not filed.
- B6 FAIL at the tip. Swap ask reproduced green in my window (demo backend 4211, 200 USDC swap over the $100 threshold): No, Yes, fold toggle in view at 400x800, 400x780, 860x800, 860x700 (stage rows "0px 669px", the ask takes the stage), 1280x800 (col 384) and 1180x780 (col 360), dock body scroll 0, overflowX 0. Send card (150 USDC to someone-else.near, confirmed:true through the door) with the working tree's uncommitted `.dock-body > .dock-actions { position: sticky }` neutralised page-side: No bottom under the viewport by 70 px at 1280x800, 109 at 1180x780, 135 at 400x800, 108 at 860x700 (shots/dock-send-400x800-tip-css.png). With the uncommitted sticky row the answer stays on screen, but Copy in the body is still out at 860x700 (715-745 in a 700 viewport).
- B7 FAIL on one family. `node --test tests/unit/design-contrast.test.ts`: 5/5 green on the shipped colourway, the lightest ground and the dimmest accent. But src/view/theme.ts:214 holds `down` to MIN_MARK_CONTRAST (3); `applyPatch(DEFAULT_THEME, { down: '#a0505c' })` returns ok (3.46:1 on #0e0f13) and set_theme exposes down (mcp.ts:836), so "Turn off" (.btn-danger, components.css:106) and "Freeze everything" (.btn.freeze, layout.css:320) sit at 3.5:1 on an accepted theme. The report names it (request 4); the fix is one token at theme.ts:214, outside this node's files.
- 3.6 PASS. Basic, Pro, Trade, Vault at 400x800 and 860x700: overflowX 0, no clipped text node (DOM probe, sr-only excluded), no button overlap on screen (shots/*-400x800.png, *-860x700.png); bar wraps to three rows at 400 without overlap; vault chips carry no dot. Known: the swap card's address breaks 41+1 at 400 (report request 2).
- 11.4 PASS. Detached worktree at 3da97fd^ = 6ff58a9 with the four new test files copied in: design-buttons-ui 11 of 12 red (the [hidden] guard is pre-existing, green), design-dock-ui 4 of 4 red, design-contrast 1 of 5 red (hover/press), button-inventory 3 of 7 red. All green at the tip.
- Counts: `npm run typecheck` exit 0; `npm test` 3096 tests, 3096 pass, 0 fail (scripts/scratch/review-e-correct/test.log). Detector: `impeccable detect --json ui/` = []; `--no-config` = 4 (overused-font geist, Karim's shortlist; two clipped-overflow-container on html/body in index.html; one hairline+shadow advisory). The two node E ignores are file-scoped to ui/index.html with reasons that match the code (layout.css:9 html/body overflow hidden by design; tokens.css:129 "two shadows in the whole app"): reasoned, not a silence.

3. Frozen rules touched by the diff: only ui/design/*.css, ui/index.html (freeze button attrs), .impeccable/config.json, tests, scripts. Rule 12 holds: No/Yes untouched (decision.js unchanged), nothing pre-answered; the stacked ask rule hides the world row (layout.css:536), not the gate. Rule 7 holds: `[data-pending="true"] { pointer-events: none }` and `.btn:disabled` only dim, no CSS enables a button. Rule 10 holds: tests are source assertions on the shipped sheets, no test-only values. Rule 11 holds: 4 local commits, no attribution lines (`git log main..rfp/e-craft --format=%b`).

4. Findings a person would hit
- A pending ask stays off the dock after the backup card is dismissed until the next state change or a reload (decision.js render is store-driven; seen in my window: "Not now" then dock hidden while a swap was pending). Pre-existing, node D's file.
- Send card at the default window: No/Approve under the fold (B6 above); the working tree fixes the row but Copy still scrolls under at 860x700.
- A read card in the stacked layout keeps the 46vh split, so a long read card (the "Back up now" card seen in a peer's window) has its primary button under the fold at 400x800; only asks get the whole stage.
- The transcript's fade under the dock leaves a sliver of the logo above the dock's top edge at 860x700 (dock-ask2-860x700.png, y about 415).
- 3 of the 4 non-.btn pressable families the report calls "hover, active" drawn (chip, bar-state) are found by the scan only via exact-class selectors; the scan misses type-qualified rules (button.chip:active exists) and role=button pressables (2 sites), so the inventory table under- and over-counts states.

5. Claims not reproduced
- "design-buttons-ui ... 10 of 11 red on the parent": the file has 12 tests, 11 red on 6ff58a9.
- "B6 PASS ... every button in the dock ... at a 400 px column": true for the swap ask only; false for the send card (measured above).
- "B1 ... 0 problems ... label plus 24 px": true only with the undisclosed btn-quiet exemption.
- Note: the worktree is being edited by the builder while under review (uncommitted layout.css, sendcard.css, tests, inventory, four new chat-send captures); my measurements ran against the tip's rules by neutralising the uncommitted sticky rule page-side. Shared-browser incident reported to the lead separately (peer tab on 4215 clicked through its first run by me).
