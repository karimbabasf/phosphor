# Review A, security: the relay swap rail (rfp/a-relay, tip f2a5907 = merge of main into c0232de, base eb28109)

1. Verdict: ACCEPT. No Critical, High or Medium. Two Lows, both wrong-number or wrong-verdict shapes with no attacker path to funds; the signing path holds every row of the spec's table on the string it signs, and reconciliation holds no key.

2. Per criterion (scratch suite: `node --test scripts/scratch/review-a-secure/review-a-secure.test.ts` -> `tests 21, pass 21, fail 0`; full: `npm test` -> `tests 3143, pass 3143, fail 0` incl. injection + lockdown; `npm run typecheck` exit 0)
- 1 PASS on quote/floor/zero/bigint, FAIL on the truncation clause (Low, inherited). Floor read off the draft, zero refused before any read (intents-relay.ts:226, test "R2: minAmountOut 0": quoteCalls 0, saltReads 0, signed 0). One unit under the floor at execute -> held, signed 0 (intents-relay.ts:365; "R2: one base unit under"); at the floor -> signed with diff USDT "1950000". Compare is bigint from decimal strings (payload.ts:143-148, 234, 240, 243): floor 10^24+1 vs offered 10^24 refused "below the ... floor" while `Number()` of both is equal. The diff is the price: payload.ts:243 requires receive === chosen quote. BUT src/intents.ts:230 `toBaseUnits` = viem `parseUnits`, which rounds half-up: `toBaseUnits(1.9999995, 6) = 2000000`, `toBaseUnits(5.934637, 5) = 593464`. The floor is raised (never lowered) by at most one base unit, and amountIn can be rounded up one base unit; pre-existing on main, same call in intents-native.ts. Fix: cut the decimal string at `decimals` before parseUnits; test `toBaseUnits(1.9999995, 6) === 1999999n`.
- 2 PASS. Every row refuses with its own sentence, 35 mutations in test "check table: every row": verifying_contract payload.ts:183; signer_id lowercased (checksum case refused) :187-191; nonce 32 bytes V1, reused-in-process refused :193-200 (also rail test "one nonce per process": second execute with repeating random rejects before the key, signed stays 1); deadline absent/past/120001 ms :202-208, nonce must outlive it :209; exactly one token_diff, no stray key :211-221; exactly two diff keys :227; negative side = draft asset and amountIn to the unit (off by one, "-0" refused) :229-236; positive side >= floor and == quote :238-245; assets differ :228 + builder :102; JSON.parse once :168, duplicate key refused via duplicateJsonKey (intents-sign.ts:73) :175, numbers/"1e6"/"+n" refused :143-148. Nonce layout confirmed against near/intents main (contracts/defuse/core/src/nonce/versioned.rs:12, README "Versioned nonce"): magic 5628f6c6 (4) || version 00 (1) || salt (4) || deadline i64 LE ns (8) || 15 random = 32 bytes; README vector reproduced byte for byte. Contract side (core/src/engine/mod.rs:90-108, cached.rs:172-175): salt must be valid, intent deadline <= nonce deadline, nonce unexpired, NonceUsed refused. Deadline never past the quote: payload.ts:79-83 min(expiry, now+120 s), test "deadline: capped at the quote expiration" (30 s quote -> deadline = NOW+30 s; 600 s quote -> NOW+120 s).
- 3 PASS. Test "one signature": no reply once -> 2 publishes, identical payload+signature, 1 signature; no reply twice -> stops, unconfirmed with nonce; FAILED -> 1 publish, providerStage NOT_FOUND_OR_NOT_VALID; an answered 500 -> 1 publish (publishOnce intents-relay.ts:150-167, noReply intents-submit.ts:33). Key never read in simulate: address() rigged to throw, simulate ok, addressReads 0, balanceReads 0, saltReads 0. Two-phase: hooks, status, after-balance and nonce view all throwing -> execute still returns with the nonce (tell :127-133, readBalance/readNonceUsed :178-179, watchStatus catch :618). Signer called exactly once in every execute of my suite and in 13 of the builder's 23 (the other 8 assert through payloadSigned or sign nothing).
- 4 PASS. Test "evidence order": evidence[0] = {nonce, deadline, relayQuote.quoteHash} at publishes 0 / statusCalls 0 (intents-relay.ts:429); evidence[1] = {txids:[hash], handle} at publishes 1 / statusCalls 0 (:459). Persist is synchronous (lifecycle.ts:211 -> store.ts put, renameSync) so the nonce is on disk before publish. Killed between the two: test "reconcile: ... tripwire": a row with nonce+deadline and no handle is reconciled by the nonce alone (only `nonce:` reads asked; a rail whose execute throws is never called): unspent 6 min past deadline -> failed "never executed and nothing left the balance"; spent -> executed; unspent 1 min past -> waits. View names confirmed in the contract source: `is_nonce_used(account_id, nonce: AsBase64<Nonce>) -> bool` (contracts/defuse/src/accounts.rs:29), `current_salt() -> Salt` hex (contracts/defuse/src/salts.rs:20); verifier.ts:73,80 call exactly these.
- 5 PASS. Client types and bounds every field (client.ts:76-92, :94-106): test "hostile wire shapes": numeric amount_out, "1.9e6", "-5", unparseable expiry all dropped; `<script>` intent hash -> "gave no intent hash"; unknown publish status -> error, not re-posted; status "MAYBE_LATER" passed up as itself and never terminal (RELAY_TERMINAL :79, test "unknown status word": row unconfirmed, 1 signature). Nothing from the response reaches the payload but the two amounts and ids after the draft check (pickQuote payload.ts:275-282, plan :231-243). Hostile quotes via the rail: amount_in 2000001 -> held, signed 0; expiration in the past -> held, signed 0; third asset with a huge amount_out passed over, honest smaller quote signed with the draft's two ids; pair swapped -> held.
- 6 PASS. Test "API key": header X-API-Key only, only when non-empty (client.ts:116-120), env fallback PHOSPHOR_1CLICK_API_KEY (:110); a thrown relay error carries "server exploded", not the key. `grep -rn 'apiKey\|X-API-Key' src scripts ui`: no log, audit or detail line carries it; src/runner/host.ts:49 strips it from the agent's env.
- 7 PASS. `git diff main...rfp/a-relay --name-only`: nothing under src/policy, src/proposals/draft.ts, lifecycle.ts or the decision routes; execute.ts changes one line (pickEvidence relayQuote). The always-click rule keys on `draft.kind === 'swap'` (draft.ts:175-197), venue-agnostic. Config adds only `swap.rail` z.enum(['relay','oneclick']) strict (config.ts:95-101): backend on 4218 booted with `{"swap":{"rail":"relay"}}` (health ok), refused `"auto"` ("Invalid enum value") and `"autoApprove":true` ("Unrecognized key"). An unpriced swap values as Infinity (intents-relay.ts:308-310).
- 8 PASS. gitleaks over the branch history (`--log-opts eb28109..rfp/a-relay`, 10 commits) and files: 3 hits, all false positives (the env var NAME at client.ts:23 and security-audit.md:22; the public USDC contract id at intents-relay.test.ts:55). trufflehog: 0 verified, 0 unverified. Evidence files carry no 0x+64hex, no secp256k1:/ed25519: material. Only key in tests is the documented throwaway `0x22..22`. scripts/relay-probe.ts imports no signer or keystore.
- 9 PASS. reconcileRelaySwap (reconcile.ts:407-525) reads lookup.status and lookup.nonceUsed only; reconcileOnBoot persists status and detail (:113-131). Tripwire test above: execute/simulate never called across boot sweep + reconcile. Row past deadline + 5 min grace with nonce unspent -> failed, nothing lost (:524).

3. Frozen rules touched: 2 (holds at the signature, payload.ts:240/243, intents-relay.ts:365; truncation clause does not hold, src/intents.ts:230, Low, pre-existing). 1 (holds: no policy/threshold file in the diff; config enum strict). 4 (holds: gitleaks/trufflehog clean, no key in tests or evidence). 7 (holds: rail choice is config, both rails built, rails/index.ts).

4. Findings the criteria do not name (security-audit skill, by severity):
- Low, reconcile.ts:517-524: `failed` on "unspent past deadline" trusts is_nonce_used alone, but the contract prunes a SPENT V1 nonce once its salt is rotated out (contracts/defuse/src/garbage_collector.rs:41 `deadline < now || !is_valid_salt(salt)`; README: admins rotate "in extreme cases"). Path: salt rotated inside the 7-day window -> spent nonce reads false -> row says "never executed, nothing left the balance" -> the person swaps again. No theft, one unintended second swap. Fix: ask `is_valid_salt(nonce.salt)` (view exists, salts.rs:17) before the failed verdict; no verdict when false, like the nonce-life branch.
- Info: on a hold, execute says "no solver offered a price" even when the relay answered with a mismatched quote (intents-relay.ts:361 drops pick.passed; simulate keeps it :317). A person retrying for 5 min sees no reason.
- Info (tools): semgrep 3 hits, all src/transactions.ts:517-519 non-literal RegExp on an escaped symbol, pre-existing, no ReDoS path. osv-scanner: fast-uri, hono, qs (transitive via @modelcontextprotocol/sdk), pre-existing, not this rail's.

5. Claims in report-a not reproduced: none. 3143/3143, typecheck 0, 75 added tests (relay-payload 21, relay-client 8, intents-relay 23, reconcile-relay 16, relay-demo 3, rail-wiring 4) all reproduce; "deadline never past the quote expiration" reproduced by my test; "signer called exactly once in every test" is asserted in 13 of 23 rail tests, the rest through payloadSigned. Note for the lead: the brief named tip c0232de; the branch tip is f2a5907 (main merged in), clean tree. Backend on 4218 quit (0 listeners); scratch data under scripts/scratch/review-a-secure/ (the app refuses a keys path inside the repo, so PHOSPHOR_KEYS pointed at a never-created scratchpad file; a peer's demo keys.enc.json already sat in the shared scratchpad and was not touched).
