# Review B, secure (rfp/b-hyperliquid at 96ef9f7, diff main...rfp/b-hyperliquid, 22 files)

1. Verdict: REJECT. One Medium on the money-leaving path: a send the venue took but did not answer can land the row `failed` with no nonce or handle on it, which is the state the code itself calls "how a second copy gets signed". Everything else on the brief holds; the fix is two small edits in hypercore-withdraw.ts.

2. Per item
- 1 sendAsset signing: PASS. Fetched signing.py, exchange.py (hyperliquid-python-sdk master) and the docs exchange-endpoint page: SEND_ASSET_TYPES field order (hl-user-signed.ts:114-125), domain HyperliquidSignTransaction/1/421614/0x0 (103-108), primaryType, `amount` as a string, `fromSubAccount` "", nonce = top-level nonce in ms: all match. Vectors: `npm test` "the sendAsset payload signs to the SDK vector on Mainnet" and "on Testnet" green. Amount: one formatter, `toAmountString` (144-154): `toFixed(6)` on the draft number with a round-trip guard; scratch grid of 95,286 six-decimal amounts, 0 refused, so no value the card could carry is re-rounded on the way (`money()`/`usdc()` toFixed are sentence-only). Destination: 1Click's `depositAddress` only after `quoteSignatureProblems` with the pinned ONECLICK_MANAGER_PUBLIC_KEY (quote-signature.ts:41; `quoteKey` is never wired from env, grep) plus the echo binding recipient = the key's own account (hypercore-withdraw.ts:443-461, 574-584); `requireOwner` (193-205) reads the key per call. sourceDex/destinationDex: the rail passes none, the constants default (hl-user-signed.ts:636, 660). Nonce: clock once, the retry reuses `first.nonce` (hypercore-withdraw.ts:616-621), at most one retry, identical bytes; note the retry re-signs the same typed data (deterministic signature, the builder's own test line 550 says so), so "signed once" is true of the bytes, not of the key access. After the signature: FAIL on the retry path, see finding M1.
- 2 Frozen 1 and 1.2: PASS. `git diff main...rfp/b-hyperliquid -- src/proposals src/policy` = 0 lines; execute.ts:27-35 downgrade to needs_approval for hl_withdraw untouched; lifecycle.ts:477 `evaluate()` then `land()` for every kind. Env reads added in the src diff: only the five PHOSPHOR_DEMO_* knobs, each behind `cfg.mode !== 'demo'` (demo.ts:86, 103); nothing auto-approves.
- 3 1.10 and frozen 8: PASS. Withdraw card: `feeUsd` = quote inside-fee + HL_ACTIVATION_USDC always (hypercore-withdraw.ts:368, 393), so the card never under-states; execute charges the fee only when `userRole` says `missing` (hl-user-signed.ts:627) and plan() requires balance >= amount + 1 before any quote (293-295). A wrong `userRole` word can only make the venue refuse a short send, never charge past the card.
- 4 8.1: PASS on the withdraw, Low on the deposit. Withdraw: card `arrivesAtLeast` = `usdc(draft.minReceived)` (392), check `guaranteed < toBaseUnits(minReceived, 6)` (426-427) as bigint; scratch grid: card above the check floor in 0 of 95,286 amounts. Deposit: card `money(draft.minCredited)` rounds half-up at 6 dp (hypercore-deposit.ts:332, 339-340) while the check is at 8 dp (391 `guaranteed < p.minCreditedBase`): card above the check in 47,044 of 95,286 amounts, by at most 50 base units (5e-7 USDC); example amount 5.041874: card 4.571707, check 4.5717065. Finding L2.
- 5 Frozen 9: PASS. 0 added lines mention relay; both rails on `oneClickClient`/`spendFromIntents` (hypercore-withdraw.ts:166, hypercore-deposit.ts:641).
- 6 Preflight and hold: PASS. intents-spend.ts:184 refuses (memo and floor via checkQuote, hypercore-deposit.ts:418-424) before the preflight at 200-220, which returns `{signed:false, held}` before `generateIntent`; rails/index.ts:86-89 wires it for hl_deposit; HELD_RETRY_MS 30_000 and HELD_MAX_MS 15*60_000 (execute.ts:224-225) unchanged, `ctx.held` unwired in live.
- 7 Reconciliation: FAIL on one path. Boot never re-executes (reconcile.ts:88-128; scratch crash test: rail executions 0). But a withdraw killed while polling carries evidence and no pocket (execute.ts:310-318 write the result only; the pocket lands at 390-402 when the rail returns), and the sweep then writes `executed` on 1Click SUCCESS alone (reconcile.ts:286). Scratch crash-no-pocket.test.ts: after boot `needs_reconciliation pocket: none`, after a SUCCESS sweep `executed ok: true stage: confirmed` with the verifier balance still 0. Finding L1.
- 8 Demo seams: PASS. `demoKnobs` returns KNOBS_OFF and `demoHeldTiming` null unless `cfg.mode === 'demo'` (demo.ts:86, 103); `demoRails` throws otherwise (163); mode is the one enum MODES ['live','demo'] (config.ts:56, 166-177); rails/index.ts:62 builds demo rails only under it.
- 9 Frozen 4: PASS. `gitleaks git --log-opts=ae0caf3..rfp/b-hyperliquid`: 12 commits, no leaks. trufflehog over the 14 scanned files: 0. Regex sweep of every changed file for 64-hex, BEGIN, mnemonic: only SDK signature r/s vectors and the SDK's public fixture key 0x0123...0123 (hl-user-signed.test.ts:39). hypercore-probe.ts --account uses keysPath /nowhere and a signer that throws.
- `npm test`: 3098 pass, 0 fail, exit 0 (injection and lockdown suites included).

3. Frozen rules touched
- 1: holds (execute.ts:27-35 unchanged). 2: holds where it matters; note `toBaseUnits` is viem parseUnits and rounds half-up (4.7300009996 -> 4730001), which makes the guarantee check stricter, never looser; the deposit card floor is the rounded-up side (L2). 3 spirit: holds (item 1). 4: holds (item 9). 7: holds, `requireOwner` and `accountSummary(deps, own)` resolve the key per read, `isAddress` on the deposit address before the post (hypercore-withdraw.ts:581-584). 8, 9: hold (items 8, 5). 10: bent by hl-crash-recovery.test.ts:329-353, which seeds `pocket: withdrawPocket` on a row the executor never writes a pocket to at that stage, so the test passes a state the product does not produce (L1).

4. Security-audit findings (skill run: threat model, exploit paths, semgrep 210 rules on 14 files 0 findings, trufflehog 0, gitleaks 0; raw output moved to the scratchpad, worktree clean)
- Critical 0, High 0, Medium 1, Low 2, Info 2.
- M1 hypercore-withdraw.ts:616-629 with hl-user-signed.ts:637. Path: /exchange gives no reply (30 s timeout) -> `first.ambiguous`; ledger read empty (620); retry `sendAsset` re-runs `accountSummary`, which is the one unguarded read in sendAsset; a 429 or dropped socket there throws out of `execute` before `tell(hooks, {handle, nonce, quote})` at 629 runs. Executor: `rail threw` (execute.ts:331-334), `moved` false (369), row `failed`. The venue may have taken the send; the row has no handle or nonce, the sweep cannot re-check it, and a person reading "failed" proposes again: a second real withdrawal (two activation fees, collateral leaves twice; lands in the app's own balance, so no theft). Scratch retry-throw.test.ts reproduces: `rail threw: hyperliquid clearinghouseState failed: 429`, `evidence hooks fired: []`. Fix: call `tell(hooks, { handle, nonce: String(first.nonce), quote: signedQuote })` the moment `first.ambiguous`, before the ledger read; wrap the retry in try/catch and treat a throw as "unconfirmed, same nonce". Check: the scratch test's two asserts go green; hl-crash-recovery stays green.
- L1 reconcile.ts:286 confirms a pocket-less withdraw on SUCCESS alone (item 7). Fix: for `kind === 'hl_withdraw'` without a pocket, take a verifier read and keep `needs_reconciliation` with the nonce, or carry `intentsBefore` into the first `tell` so the row has its pocket from "Sending it". Check: crash-no-pocket.test.ts asserts `needs_reconciliation`.
- L2 hypercore-deposit.ts priceLines `money()` rounds half-up; card floor can exceed the 8 dp signed check by up to 5e-7 USDC (item 4). Fix: truncate to 6 dp before formatting, as cards.js does since 42f5809. Check: rounding.ts prints deposit "card above check 0".
- I1 the retry signs the same typed data a second time (deterministic, same bytes). I2 classic-account `moveToSpot = needed - spotUsdc` (hypercore-withdraw.ts:311) is a double; `toAmountString` refuses 69,722 of 219,344 amount/spot pairs after the live quote was minted, with "needs more than 6 decimals" in the person's sentence. Unified accounts (Karim's) never hit it.

5. Findings a person would hit: I2 above; the L1 card reads "Confirmed" for a crash-recovered withdrawal before the balance shows it.

6. Claims not reproduced
- report-b 8.3 "Confirmed only after the balance rose" and 8.5 "a later SUCCESS from the sweep still waits for the verifier": true only when the row carries a pocket, which a row killed while polling does not (crash-no-pocket.test.ts, above).
- evidence-b/security-audit.md "nothing throws after the signature" is not claimed there but the audit lists the retry as safe: retry-throw.test.ts shows the throw.
Scratch files: scripts/scratch/review-b-secure/ (gitignored). No backend and no browser were started for this review.
