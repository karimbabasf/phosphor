# Review B, correctness. Branch rfp/b-hyperliquid at 96ef9f7 (merge of main over dd7bf2b), base ae0caf3.

1. Verdict: ACCEPT. Every criterion in the brief reproduces with commands on my own backend (4219, data under scripts/scratch/review-b-correct/), the sendAsset bytes match the official SDK 0.24.0 run fresh, and the four regression tests are red on the parent for real assertions.

2. Criteria
- Build: `npx tsc --noEmit` exit 0; `npm test` 3098 pass 0 fail; `npm run eval` 29 pass 0 fail 0 xfail 0 error (S12, S26 pass).
- 8.1 PASS. One value, draft.minCredited: set at src/proposals/rails.ts:135 (minCreditedForHypercore); card floor hypercore-deposit.ts:339-340 `arrives`/`arrivesAtLeast: money(draft.minCredited)` read by view.ts:445-450 amountOutOf; gate :304 `minCreditedBase = toBaseUnits(draft.minCredited)`, :380-391 `guaranteed = baseUnits(quote.minAmountOut)` refused if `< p.minCreditedBase`, run on the live quote at intents-spend.ts:184 before generate (:223) and sign (:252). Tests "the simulation carries the fee facts the card draws" pass in both rail files. Venue credit at or above the floor is the lead's live proof (account empty today).
- 8.2 PASS. Withdraw facts hypercore-withdraw.ts:~395-415: total = insideQuote + 1 USDC activation, always priced (every 1Click address is fresh). My walk on 4219 before the click: `view.money {"feeUsd":"1.25","amountOut":"19.67"}`, `send.activity` "Routing 0.2 USDC and a 25 bp app fee (0.05 USDC) ... 1 USDC on top"; after: amountOut 19.75 = 20 - 0.25 to 4 dp (`equal to 4dp: true`), venue account down 21. Landed is amount minus the in-quote parts; the activation leaves the venue account beside the amount (HL docs: destination credited in full), which the card's total carries.
- 8.3 PASS. Poll: first status read before any sleep, then 3 s (intents-spend.ts:273-297 for the deposit, hypercore-withdraw.ts:175-176, 497-512 for the exit); SUCCESS label "Waiting for the venue to credit it" (view.ts:124); withdraw confirms only when `watchRise` shows the floor (hypercore-withdraw.ts:784-800, INTENTS_SETTLE 90 s, settle.ts:21), otherwise `settling: true` with the pocket (:695-707). Demo clocks on 4219: submitting 03:49:08.048 -> KNOWN_DEPOSIT_TX 03:49:10.067 (2.0 s) -> SUCCESS 03:49:14.105 -> crediting -> confirmed 03:49:18.624 with detail "rose by 19.75 USDC ... at or above the 19.67 USDC floor". Tests "first status read follows the submit with no sleep", "success ... settling with the intents pocket", "third read after SUCCESS is a confirmed withdrawal" pass.
- 8.4 PASS. `row.stageAt` keys in order on my row: waiting_for_you, signing, submitting, KNOWN_DEPOSIT_TX, PROCESSING, SUCCESS, crediting, confirmed; one id throughout; labels Sending it, Deposit seen, On its way, Waiting for the venue to credit it, Confirmed. Nothing in the rails prompts the agent.
- 8.5 PASS. Timeout path returns ok:false with `evidence: { handle, nonce, quote }` and the intents pocket (hypercore-withdraw.ts:723-740); executor: moved = txids or handle or nonce -> needs_reconciliation, never failed (execute.ts:371-379). My kill -9 at PROCESSING on 4219: on disk `status executing | nonce 1789962641048 | handle demo-8e6567b8e85b | txids [0x91e9...]`; after boot `needs_reconciliation`, same nonce, handle, hash; past the demo deadline `stalled "Late, nothing has changed"`, settlesForward true. hl-crash-recovery: 12/12 pass.
- 8.6 PASS. Docs fetched (data): HL exchange-endpoint "Send Asset": type sendAsset, fields hyperliquidChain, signatureChainId, destination, sourceDex, destinationDex, token, amount, fromSubAccount, nonce, outer nonce equal; SDK 0.24.0 signing.py SEND_ASSET_SIGN_TYPES in that order, primaryType HyperliquidTransaction:SendAsset, domain HyperliquidSignTransaction v1 chainId 0x66eee (421614) zero contract, matched by hl-user-signed.ts:100-107, 146-157. The SDK ships no sendAsset vector, so I ran the SDK in a scratch venv (pip 0.24.0, released 2026-06-04): Mainnet r 0xfe1a04..dd04 s 0x3dbf44..9d39 v 28, Testnet r 0x99a9ac..f60e s 0x5d2db4..dcc0 v 27, byte for byte the pinned values; the old spotSend control reproduced too. 1Click hyperliquid.md: "sendAsset: Supported. Spot or perp". No non-comment spotSend/usdSend left under src (grep: one stale comment, src/hl/exchange.ts:45). A venue `status: "err"` reply returns "sendAsset refused by Hyperliquid: <venue sentence>" (hl-user-signed.ts:514) and the rail lands `ok:false ... Nothing was sent.`, txids [] (hypercore-withdraw.ts:648-655); test "an HTTP 200 carrying status err is a failure" passes. Short balance names the most: probe "the most that can come back now is 0 USDC (5 USDC floor)".
- 8.7 PASS. Tests "a quote that asks for a deposit memo is refused before anything is signed" (signed 0, generated 0) and the demo floor test pass; my call on 4219: `propose hl_deposit 2 -> policy_refused | stage refused`, "2 USDC is below the 7 USDC floor ... so 7 in is what guarantees 5 lands", result null.
- 1.10 PASS with a note. Before the click the view carries money.feeUsd "1.25" (total, activation inside) and simulation.send.feeUsd 1.25, send.activity naming the three parts, summary lines cost/routing/app fee/activation. The chat card prints the one total (cards.js:871, 945); the parts are not itemised on it.
- 2.7 PASS. etaSeconds and typicalSec 180 = TYPICAL_SEC.hl_withdraw (view.ts:220-222); summary "usually within 3 minutes end to end (the router's leg about 35s ...)"; the late row above reads "Late, nothing has changed".
- 4.1 PASS. tests/unit/hl-crash-recovery.test.ts: deposit and withdraw at Signing, Sending it, Deposit seen, On its way, Waiting for the venue to credit it, Late; each asserts hashes, handle, nonce, quote kept, executed count 0, confirm only on the venue or verifier read. 12/12 pass.
- 11 PASS. Detached worktree at 6ff58a9 (parent of 2224ab7), branch test files copied in: red for real assertions on "a success ... settling with the intents pocket", "fee facts ... total with the activation fee", "deposit memo is refused", "short balance refusal names the most"; the SDK-vector test fails to link there (no SEND_ASSET_TYPES). All five green on the tip. Worktree removed after.
- Live S12: evidence-b/eval-live-S12.txt is a real run log (grader self-test 25, three staged repos, judge f2/r2, f2/r2, f1/r2 "Landing named only as NEAR Intents"), 2/3 as claimed. The log does not name the commit (report says 1f207f5).
- Builder's facts: floor 7 in, 5 lands: MIN_DEPOSIT_USDC = 7 (hypercore-deposit.ts:101), HYPERCORE_VENUE_MIN_CREDIT_USDC = 5 (:92), minCreditedFor(7) = 6.522 >= 5 (:126-129); the live quote expects 6.67. Intents holds no USDC: my probe run, `INTENTS no USDC held`, account unified 0.000775 USDC, withdraw 7: 6.782534 expected, deposit 7: 6.666488 expected.

3. Frozen rules touched
- 1: withdraw still forced to a click, execute.ts:27-34 unchanged. Holds.
- 2: floors off the quote in base units, hypercore-deposit.ts:380-391, hypercore-withdraw.ts:426-429; refused before signing. Holds.
- 4: key never on a result; test "no result or action ever carries the private key" passes; probe --account reads no keys. Holds.
- 5: facts ride on SimulationResult.send, view.ts unchanged, no second stage table. Holds.
- 8: every demo knob gated on cfg.mode === 'demo' (demo.ts:180, 199), test "a mainnet boot holds no demo rail" passes. Holds.
- 9: both rails still quote and route through 1Click; appFeeBpsOf only reads the echo (intents-spend.ts:478-488), no fee added. Holds.
- 10: scripts/eval.ts, the judge and src/role.ts untouched by the diff (`git diff --stat` empty). Holds.

4. Findings the criteria do not name
- judgeSettling closes a settling row as `failed` when the pocket rises by less than the floor (execute.ts:574-581). The withdraw now carries an intents pocket (new here), so any unrelated USDC credit inside NEAR Intents during the settle window ends a live withdrawal as failed while 1Click is still routing it. Pre-existing rule, new exposure.
- "Late" is stamped at DEADLINE_SEC = 1440 s for HL kinds (view.ts:232-236, execute.ts:594-624), not at the 180 s poll timeout: for 21 minutes a stuck live move reads its last router word, unconfirmed.
- The card's landing leg reads the floor (19.67) while the summary says about 19.75 until settlement; the builder's request 1 (an "at least" line in cards.js) is the fix. Reproduced.
- Demo only: the boot sweep writes "No venue handle was recorded for <hash>" over a row that carries a handle (reconcile.ts:334-347, oneClickStatus undefined in demo, main.ts:491-494). Reproduced; request 3 in the report.
- The worktree holds an untracked docs/superpowers/prompts/ready-for-people/review-b-secure.md (another reviewer's file inside the repo).

5. Claims not reproduced: none failed; the live S12 run was not re-run (live eval spends model calls), judged from its log only.
