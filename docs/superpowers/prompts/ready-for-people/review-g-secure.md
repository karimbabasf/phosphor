# Review: node G, secure (branch rfp/g-launch, tip 4240cd0, backend on 4214, data under scripts/scratch/review-g-secure/)

1. Verdict: ACCEPT. No key material anywhere, sweep clean on G's files (history trips only on node B's later demo hashes), gitleaks hits all public, rules 4 and 11 hold; the new out-wall covers 2 of 4 audit exits (finding 1) and the bug form asks for a log that carries addresses (finding 2), both fixable after merge.

2. Per question (criterion in brackets)
Q1 [13.7, rule 4] PASS. Booted `node src/main.ts` (demo, port 4214, PHOSPHOR_KEYS outside the repo: config.ts:285 refuses a key path inside the tree). Planted every shape through the real writer (MCP tool_call args, mcp.ts:303) and read back:
   shape          /api/log  log_tail  audit.jsonl on disk
   seat secret    gone      gone      present      window token  gone  gone  present     privateKey/mnemonic/seed field  gone  gone  present
   bare 64 hex    kept      kept      (by design, log-tail.ts:19-23)   0x+64 hex, secp256k1:/ed25519: base58, Bearer, sk-ant/sk-proj, 12 BIP39 words in a plain field, apiKey/api_key field  kept  kept
   Redaction is on the way out only: the file keeps the values and every `prev` link still verifies (7 lines checked). Both secrets are 64 lowercase hex from mintToken (auth.ts:31, backend.rs:236), inside CREDENTIAL_RUN's class (log-tail.ts:54); the holders compare hashes with no side effect (agents.ts:251-257, auth.ts:149-153).
   `diagnose` is a real gap: wallet.ts:347-351 maps `${e.ts} ${e.type}: ${e.msg}` through `withoutAddresses` only, no `redactEvent`. Narrow (msg only, rows filtered by data.id), same class as finding 1.
Q2 [1.7] PASS. Planted 21 shapes in a file staged into a COPY of the index (GIT_INDEX_FILE, the shared index untouched), `node scripts/sweep.ts` -> FAIL as it should: caught hex64 (raw, 0x, uppercase), base58 88, ed25519:, PEM, mnemonic whole-line and double-quoted, 24 words. Missed: secp256k1:, sk-/ghp_/Bearer (no pattern, gitleaks' job), mnemonic in single quotes, backticks, trailing comma or comma-separated (WORD_RUN, sweep.ts:92 and the `"..."` candidate at :323, pre-existing on main:168). KNOWN_PUBLIC_FORMATS: a fresh key on `checksum = "<hex>"` in any file named Cargo.lock is EXCUSED (sub/Cargo.lock:4), the same key on another line of that file trips (:5); the report states this trade. BIP39 change only tightens (viem list). Allowlist: 61 new entries, sampled 16 across the list, each found where its note says and each is an `assert.equal(r/s, ...)`, a fixture recipient, a public tx, a release sha256 or a probe telemetry id; none is a key. Clean tree now: `npm run sweep` 5 of 6 PASS (811 files), git history FAIL on 3 demo-rail hashes node B committed in a8d67ca on rfp/b-hyperliquid after G's report (evidence-b/demo-kill9-4202.txt:5-9, demo-withdraw-walk-4202.txt:26-27, random demo txids, not secrets); G's own files are clean and the report predicted exactly this for the merge.
Q3 [13.4, 13.6, 13.7] PASS. main.rs `report_url` = base + `&version=` + `&os=` (package version, `sw_vers -productVersion`), RFC 3986 percent-encoded; no user, path, address or secret. Copy Log = `GET /api/log?limit=200` through `request_within` with a bare head (no token on the wire), the redacted route, never the file; JSON array only. Outcome on the item title (`say_on_menu`), zero new `dialog(` in the diff. `cargo test --offline`: 24 passed, 0 failed, the 3 new ones green.
Q4 [13.7] PASS with finding 2. config.yml lists private reporting first; bug_report.yml says "Never paste a key, a seed, a seat secret or a full address you care about" and SECURITY.md keeps the advisory path. The `log` textarea does ask for the redacted tail in public; see finding 2.
Q5 [13.3] PASS. Added doc lines: no EVM address, no 64-hex but release sha256s, no `.near` account, no ticket, gist or incident word. `git grep cf299a`: only the lead's prompt file line 42, truncated, not in this diff. `fae3c9`: same line only. Evidence files print `/Users/karimbaba/...` paths (5 files), the repo's existing evidence pattern.
Q6 [rule 11] PASS. 9 commits main..rfp/g-launch, `git branch -r | grep g-launch` = 0, `git ls-remote --heads origin` = 0, `git tag --contains rfp/g-launch` = 0, local tag set identical to origin's (12), the one workflow change is a comment line (ci.yml:46), no site path in the diff, no attribution line in any commit body, main is 32 ahead of origin/main and 0 behind.
Q7 [1.7] PASS. `gitleaks version` 8.30.1, no .gitleaks.toml. `gitleaks git --redact .`: 181 findings, all generic-api-key, 130 in tests/fixtures/poa-tokens.json, 15 in data/tokens.json; `detect --no-git` 188 outside node_modules and scratch. Sampled 18 by commit and line: token contract ids, `RELAY_API_KEY_ENV`, `'a1b2c3d4e5f6'` in research.test.ts, two aToken addresses in .probe/probe-crosscheck.mjs. Zero secrets.
Q8 [1.c] PASS. `npm test`: 3076 tests, 3075 pass, 1 fail, 25.8 s. The fail is docs.test.ts "the changelog opens on the version in package.json" (0.8.0 vs 0.7.0, the report predicted it). lockdown 4/4 green, injection 20 of 22 names matched green and the run's only ✖ is the docs one; log-tail 2/2, sweep 5/5, keys-path 18/18, seat-secret-log 1/1, log-fingerprint 3/3.

3. Frozen rules touched
- 4: holds. The key never enters a writer (unchanged); seat and token stripped before logging (mcp.ts:183) and now redacted on the way out (router.ts:86, wallet.ts:271). Copy Log sends no token (main.rs `fetch_log_tail` head). The key-in-memory item is untouched.
- 10: holds. The sweep test imports the script (main() guarded, sweep.ts:562); no eval-only branch, no rubric edit.
- 11: holds, evidence under Q6.

4. Findings the criteria do not name
1. The out-wall covers /api/log and log_tail but not `/api/events`: sse.ts:165-168 streams every audit event raw to any local GET, no token. Reproduced: opened the stream, wrote one line with the seat, token and a `privateKey` field through the MCP writer; all three arrived verbatim. log-tail.ts:86-87 says nothing else reads on behalf of an outside caller; the stream does. Same one-line fix as request 5 (`redactEvent` at sse.ts:168 and wallet.ts:351). /api/state carries no events (checked).
2. bug_report.yml asks for the tail in a public issue while telling the reporter never to paste a full address: the tail keeps addresses and amounts (tool_call args at mcp.ts:303, `totalUsd` at execute.ts:64, `evidence` at :316; only `diagnose` runs `withoutAddresses`). Either fingerprint addresses on the tail routes or say in the field that the log carries them.
3. SECRET_FIELDS (log-tail.ts:35-49) skips `apiKey`, `api_key`, `authorization`, `bearer`; a value under those names left both readers whole. `token` is excluded on purpose (Hyperliquid asset), the others are not named.
4. A local process squatting the configured port answers Copy Log: `fetch_log_tail` checks `HTTP/1.1 200` and a JSON array, not the x-phosphor boot nonce the window open requires (backend.rs:362). What lands on the clipboard is then a stranger's text.
5. tests/unit/diagnose.test.ts:104 holds Karim's real EVM address in full, on origin/main since 54ab3ba. An address, not key material, and not this diff; his call whether a public repo should carry it.

5. Claims not reproduced
- "176 gitleaks findings": 181 now (`gitleaks git --redact .`), the extra five came in with the merge commit 4240cd0 (evidence-a/security-audit.md and other nodes' fixtures); same classes.
- "3076 tests, 2 failing after the changelog commit": 1 failing after the merge; the demo-rail one is green on this tip (`npm test`, ✖ count 1).
- "npm run sweep PASS, 810 files, 5840 blobs": on this tip 811 files and 5947 blobs, and the history check FAILS on node B's three demo hashes (above); the lead excuses them by exact value at merge or B drops them from its evidence. G's tracked content passes.
