# Review C, correctness. Tip 291fc9a (merge of main 1a5bbc1 into rfp/c-agent). Evidence: /private/tmp/claude-501/-Users-karimbaba/c49886b8-5402-456e-8f99-4860322f1b81/scratchpad/review-c/ (shots/, backend-*.log, vendor-b/)

1. Verdict: REJECT. Every registration and paste line carries PHOSPHOR_PORT, but the proxy reads only ACC_PORT (src/mcp.ts:29), so an agent registered by this branch dials config.json's 4177 whatever port the app runs on (the exact case the branch's decision log says it fixed); and a fresh pick of a missing agent says "X is no longer on this Mac." instead of the 10.3 sentence.

2. Criteria
- 10.1 PASS. Empty data dir, demo backend 4215, first run walked in the automation Brave: 6 tiles in catalog order, 3x2 at 1280x800 (card 174..626, overflow 0), 2 columns at 400 px (grid "139px 139px", no name clipped), mark over name and colourless at rest like netpick. shots/02-picker-open.png, 18-picker-400px.png.
- 10.2 PASS. `node --test` on the 5 new files + tests/lockdown.test.ts: 59/59. Route per entry (PATH=fixtures): claude 4 ms, codex 1, hermes 4, grok 4, mcp 0, desktop 1, every state one of the four; hermes -> fake-agent-hangs.sh: 2501 ms, `installed_not_logged_in`; scan with the hang: 2503 ms. No network: agents-catalog.ts has no fetch/http; each real probe answers the same under `sandbox-exec -p '(deny network*)(deny file-write*)'` (claude auth status exit 0 loggedIn true 215 ms, codex login status exit 1 "Not logged in", hermes config get model exit 0, grok --version exit 0; control `curl https://example.com` exit 6).
- 10.3 FAIL. src/http/mutation.ts:314 `writePick` runs before :320 `checkFor`, and :119 sets `wasPicked: picked === agent`, so every pick of a missing agent reads "Codex is no longer on this Mac." (route agent-pick codex; screen shots/03-codex-not-installed.png). agent-check without the pick gives the right sentence. installed_not_logged_in and Claude Desktop sentences are exact (shots/19, 06); path only behind Details (fold hidden until the chip, shots/05).
- 10.4 FAIL on the port, PASS on the writes. Real binaries, scratch homes (CLAUDE_CONFIG_DIR, CODEX_HOME, GROK_HOME, HERMES_HOME under vendor-b/): agent-pick codex 0.12 s, grok 0.17 s, claude 2.3 s, hermes 3.6 s, all registered=true; .claude.json mcpServers.phosphor, codex config.toml [mcp_servers.phosphor.env], grok config.toml, hermes config.yaml all carry PHOSPHOR_PORT=4215 and PHOSPHOR_DATA_DIR; real ~/.codex, ~/.grok, ~/.hermes configs untouched (mtime and size identical). "Another agent" gets `PHOSPHOR_PORT=4215 PHOSPHOR_DATA_DIR=... node .../src/mcp.ts`. Only builder: `grep -rn "mcp add\|add-json\|mcpServers" src-tauri/src/*.rs ui/` = 0 hits; main.rs mcp_command reads GET /api/connection. But the registered env driven by a stdio client (probe-proxy.mjs, PHOSPHOR_PORT=4215, backend on 4215): `wallet -> The control app is not running. Start it with: npm run app`; same with ACC_PORT=4215 added: wallet rows. src/mcp.ts:29 `if (process.env.ACC_PORT)`; src/driver.ts:374 sets ACC_PORT for the in-app child only. One line in mcp.ts (not C's file) or both names in agents-catalog.ts:517.
- 10.5 PASS. Start it (fixture claude) spawned with `--settings .../operator/driver.settings.json --setting-sources= --strict-mcp-config --permission-mode dontAsk`; grok/hermes get "is signed in: start it in your terminal and it will appear here."; a stdio client named codex on the door: /api/state members [{client:"codex"}], panel "Codex is connected.", light ready, chip "Codex: Ready" (shots/16). Reached only with ACC_PORT because of 10.4.
- 10.6 PASS. Panel: chip, one sentence, Change, Check again, Your rules (shots/12); Change draws the same 6 tiles in the panel (shots/13); Check again re-probes (fixture --version count 8 -> 10 after two checks); pick while the in-app agent runs: "Your assistant is running. Turn it off in the chat, then change it here.", agent.json unchanged (shots/14); after the Turn off card the pick lands.
- 10.7 PASS. claude -> a script printing ANSI, `<script>`, NUL and 3 KB of A: sentence stays the state sentence, junk only in Details as text; toasts 0 on every read; picker and panel never call net.readable (grep firstrun.js 1038-1437, vault.js 420-570).
- 10.8 PASS. grok picked, fixture removed, backend rebooted: boot log clean, /api/health ok, agent-check "Grok is no longer on this Mac.", panel same, light off, chip "Grok: Not on this Mac" (shots/17).
- 1.11 PASS. argv above; tests/lockdown.test.ts 4/4.
- 9.1 PASS. firstrun.js:26-30 ENCLAVE_STEPS 4, SOFTWARE_STEPS 10; on screen the picker is "Step 7 of 9" after welcome.
- 9.2 PASS. One primary per state (Check again / Start it / Continue, quiet Continue before a pick) and "Do this later"; a failed check keeps the sentence and the same button; refused threshold keeps Continue.
- 9.4 PASS. Terms card up with the first run hidden (shots/00); src/terms.ts not in the diff, TERMS_VERSION '2026-09-17'.
- Known failure 6 PASS. 150 typed -> policy.json humanClickAboveUsd 150, policy_show "Ask me before anything above $150."; -5, 0, abc -> "Type a number of dollars above 0."; 1e9 and 2000000 -> "The threshold cannot go past $1,000,000.00 from here..."; route: 12000 vs cap 10000 refused with both figures, null refused, "150" string lands (shots/11).
- 11 PASS. Detached worktree at 6ff58a9 with the four test files copied: policy-threshold-route 0/1, connection-route 0/1 (agents-catalog.ts absent), firstrun-ui 0/1, vault-agent-ui 0/9; green on the tip.
- Counts: `npm run typecheck` exit 0; `npm test` 3123/3123 (the demo-rail red in the report is gone on the merged tip); `cargo check` exit 0, 0 warnings.

3. Frozen rules touched
- 1: the threshold write is a window-token POST only (router.ts POST table), through loadPolicy, AXIS_CEILING_USD and the never-asks rule (mutation.ts:129-137), savePolicyChecked schema first (policy/file.ts:76); no /api/mcp op. Holds.
- 4: GET /api/connection carries no token and answers path, port, data dir, client names only (mutation.ts:82-90); agent.json holds {agent, pickedAt} (agents-catalog.ts:641). Holds. Note: grokLogin reads and parses auth.json's contents (agents-catalog.ts:366) while the file header says "presence, never its contents"; nothing is kept or printed.
- 12: the switch gate is reshaped, not removed: refused while an in-app agent runs (mutation.ts:305), never stops it. Holds. 10(e): in-app start under the lockdown file, no vendor login stored, no network. Holds.

4. Findings a person would hit
- Keyboard or programmatic focus paints the tile in its brand colour (agentpick.css:119), so the focused Claude tile reads as picked next to the green picked Codex (shots/19).
- Details for the two unprobed entries reads "Made by Any agent that connects to MCP servers." and "Made by A chat window, with no agent on this Mac." (detailLines pushes `vendor` through "Made by").
- README.md:34 still hands out the old `claude mcp add-json` line: a second copy of the connection truth in docs (owner G).
- A Hermes pick takes 3.6 s under "Checking on this Mac." because the registration runs inside the pick (registerAgent connects the proxy).
- Reviewer note, not product: the shared automation Brave routed another session's trusted clicks ("Do this later", "Continue") onto my tab twice; reviewers need their own BU_NAME daemon or their walks contaminate each other.

5. Claims not reproduced
- "One sentence per state, the definitions file's three sentences word for word": a pick of a missing agent gives the "no longer" sentence (agent-pick codex, above); evidence-c has no not_installed screenshot at all.
- Decision "PHOSPHOR_PORT in dev too: a checkout on any port other than 4177 was handing out a line whose proxy talked to 4177": still does (probe-proxy.mjs on 4215 -> "not running").
- evidence-c/registration-live.md shows codex without PHOSPHOR_PORT and grok with an empty env table; the real writers land both (vendor-b/), so the evidence file is trimmed, not the code. shots.log line "hermes: Checking on this Mac." is the transient, not a state.
- Counts: 3122/3123 in the report; 3123/3123 on the tip.
