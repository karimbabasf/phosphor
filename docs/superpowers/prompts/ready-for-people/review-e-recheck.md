# Review E re-check (fresh context): rfp/e-craft at a21f7da

1. Verdict: REJECT (narrow). B1 and B7 are fixed and reproduced, the swap card clears B6 at every size, and netpick Escape is right. Two holes remain: on the send card the new sticky answer row sits on top of the address at scroll 0 at 3 of 4 sizes (Approve on screen, the address it approves not), and the shared press for the non-.btn families exists in the source but loses the cascade, so 3 row families still have active == hover.

2. Criteria (own backend on 4224, PHOSPHOR_NO_PARENT_WATCH=1, key file in my scratchpad because config.ts refuses a keys path inside the repo; onboarding walked in the erecheck browser; send 250 USDC to 0x8ba1f109551bD432803012645Ac136ddd64DBA72 on ethereum and swap 500 USDC to ETH parked through /api/mcp, both needs_approval over the $100 threshold; layouts measured after a forced frame, because a background tab's ResizeObserver only settles on a paint and the unforced numbers were a different layout).
- B6 PASS on the letter, FAIL on the address clause. Send: No/Approve bottom 743 of 780 at 1180x780 (col 360), 663 of 700 at 960x700 (col 400), 663 of 700 at 860x700, 763 of 800 at 400x800; row position sticky, opaque ground, z 1. Swap: No/Yes 694/780, 614/700, 614/700, 714/800; its address fact clear of the row at all four. DOM order: address before .dock-actions at all four; scrolled to the end the last line (sendcard-activity) sits 17 px above the row at all four, hit-test lands on the line. But at scroll 0 the row covers the address: 960x700 col 400 the whole address (both lines 640-678 under a row top at 614, elementFromPoint on the address hits .dock-actions); 400x800 28 px (line 1 at 704-722 half under the row top 714, line 2 whole); 860x700 the last line "4DBA 72" (603-621, row top 614). Clear only at 1180x780. The six fact rows are clear at all four. shots: scripts/scratch/review-e-recheck/shots/send4-*.png, swap-*.png.
- B1 PASS. `INVENTORY_OUT=scripts/scratch/review-e-recheck/inventory node scripts/button-inventory.ts`: "108 measurements, 0 with a problem", 54 families. Own arithmetic on inventory.json: room under 24 px only on sendcard-info (icon only, 14 px); every quiet family 26 px at 860 and 400 (receipt-copy 44). scripts/button-inventory.ts:274 has no family exemption, only `!r.iconOnly` (recipes.ts:37) and the `sentence` wrap allowance on tx receipt-row (recipes.ts:38, report-e.md:123, disclosed). Live: "Do this later" (.btn.btn-quiet, firstrun Add money) box 110.59, label 84.59, room 26, height 44, padding 12/12, nowrap, not clipped.
- B2 FAIL (row families). CDP CSS.forcePseudoState on samples built over the live stylesheets, sampled after the 160 ms transitions: steps-fold, netpick-link, fold-head: hover+active differs from hover (press wash 12 percent, scale 0.97). dock-close: differs by the 0.97 scale only, its press wash loses to layout.css:905 `.dock-close:hover { background: var(--bg-2) }` (same specificity, later file). jump-latest: hover+active identical to hover (both color-mix agent 22%): components.css:419 loses to agent.css:571. Same for net-row (deposit.css hover) and tx.receipt-row (pro.css `.activity-rows > .tx.receipt-row:hover`, 0,4,0 over 0,3,0): active == hover, measured. 10 of the 20 families have a later hover rule setting background over the press (dock-close, pane-hide, pane-show, receipt-close, lock-eye, netpick-back, activity-link, net-row, tx.receipt-row, jump-latest); the 6 compact ones keep the scale, the 4 row ones keep nothing. tests/unit/design-buttons-ui.test.ts:195 asserts the rule text exists, not that it wins.
- B7 PASS. `node --test tests/unit/theme-slots.test.ts tests/unit/design-contrast.test.ts`: exit 0, 0 not ok. applyPatch(DEFAULT_THEME, { down: '#a0505c' }) refused ("2.94:1, under the 4.5:1"); { down: '#ff5a6e' } accepted; theme.ts:232 holds down to MIN_TEXT_CONTRAST on the raised surface. green-on-black (the only colourway, mcp.ts:824) passes its own floors as a theme and as a set_theme patch; no shipped preset is refused. Side effect confirmed and larger than the number suggests: ground #101010 ok, #151515 and every grey up to #2a2a2a refused, the refusal being the fixed gate red #ff3b30 on the raised surface (4.32:1 at #151515), so set_theme now takes no grey ground lighter than about #101010.
- netpick Escape PASS by reading: netpick.js:642-646 keydown on the picker root, Escape in tokens or address stages preventDefault and stageNetworks(); on the tiles it returns without preventDefault so the deposit <dialog>'s cancel closes the card (deposit.js:154). netpick-ui.test.ts covers tokens, tiles (not taken), address, and a non-Escape key.
- Counts: `npm run typecheck` 0 errors in tracked files (3 errors, all in the gitignored scripts/scratch/review-e-secure/dock-check.ts, which tsconfig's scripts glob picks up); `npm test` 3104 tests, 3104 pass, 0 fail; `impeccable detect --json ui/` returns [] (0 findings). Tree clean at a21f7da, 7 commits over main, no attribution lines.

3. Frozen rules touched: 3 holds on the letter (the address is whole, 42 chars in groups of 4, never truncated, sendcard.css) but the sticky row hides it at scroll 0 on three sizes while Approve is clickable, which is the reading order rule 3 exists for; 12: the gate is not removed, delayed, auto-answered or pre-filled, but its address fact is covered (layout.css `.dock-body > .dock-actions { position: sticky }`); 10 holds (source assertions on shipped sheets, no test-only value); 11 holds (git log --format=%b: 0 attribution lines, nothing pushed). 1, 2, 4 to 9 untouched (src/ change is theme.ts only).

4. Findings a person would hit
- The send card at the app's default window is the only size where the address reads before the buttons; at 400x800 a person sees "0x8b a1f1 0955 1bD4 3280 3012 645A" half faded under the row and never the last 14 characters unless they scroll.
- Pressing a network row, a receipt row or Jump to latest with a mouse gives no visual press at all (active == hover).
- Hover rules in the screen sheets are wrapped in `@media (hover: hover) and (pointer: fine)`; the press rule in components.css is not, so on a trackpad Mac the cascade order above decides, not the media.
- The swap card's own address still breaks 41+1 at 400 px (request 2, pre-existing, not this diff).

5. Claims in the node's report not reproduced
- "B6 PASS ... in its reading order (under the facts, before the fold), so it is on screen whatever the card's height" (report-e.md:53): the row is on screen, the address under it is not at 960x700 col 400, 860x700 (last line) and 400x800; the report's numbers 749/669/669/763 versus mine 743/663/663/763.
- "the 20 families that had no press take one shared press" (report-e.md B2): true in components.css, false in the cascade for 10 of them (4 with no press at all), measured above.
