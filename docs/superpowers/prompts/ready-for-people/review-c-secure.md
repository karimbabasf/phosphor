# Review C, secure. Branch rfp/c-agent at 291fc9a (main merged). Backend: demo, 4216, data under scripts/scratch/review-c-secure/, scratch HOME and vendor homes; second backend 4217 for the PATH probe.

1. Verdict: ACCEPT. Every route and probe holds under replay; two Medium defence gaps and one stale evidence file, none breaks a frozen rule or a criterion in the brief. Land finding A before the release build.

2. Per question
- Q1 (1.4, 1.3) PASS. security-model.md table replayed: mcp hello/read/propose without seat 401 x3; approve wrong token, no token, no Origin, seat-as-token 403 x4; kill, view 403; unlock 403; GET /api/session 404; GET /api/log 200, token and seat absent from the body (grep 0). New: POST /api/policy/threshold no token 403, seat-as-token 403, no Origin 403, Origin null 403, foreign Origin 403, Host evil.com 403 "answers only on 127.0.0.1"; policy.json unchanged after all refusals (thr 100, cap 10000, kill false). Values: 10001, 10000, 20000 -> 400 "Asking above $X with a hard cap of $10,000.00 means nothing ever asks you"; 2000000 -> 400 "cannot go past $1,000,000.00"; -5, 0, 0.001 -> 400 "above $0"; "NaN", "abc", null, 1e400, true, [], {}, "" -> 400 "has to be a number of dollars". Body {usd:250, outbound:{maxPerTransactionUsd:999999, autoApproveDailyUsd:999999, destinationAllowlist:[..]}, killSwitch:true, patch:{..}} -> 200 threshold 250, file after: cap 10000, session 25000, daily 500, allowlist and kill unchanged (mutation.ts:155 builds the patch itself; lifecycle.ts:116 mergePatch copies named fields only). Write: savePolicyChecked runs policySchema.safeParse before atomicWriteJson (policy/file.ts:101-104). Audit: audit.jsonl `policy_changed` "human set the ask threshold to $250.00 at the window (was $100.00)" data.by "human", where "onboarding", chained by prev hash. Agent reach: `git diff main...rfp/c-agent -- src/mcp.ts src/http/mcp.ts src/agents.ts operator/ tests/tool-surface.ts` empty; injection suite 29/29 including "the MCP process holds no path to an approval, and none to the human door".
- Q2 (10.e, rule 4) PASS. GET /api/connection with no token, no Origin: 200, fields agent, name, command, registers, inApp, picked, connected (7, read one by one); token 0, seat 0, hex32+ 0, 0x 0 in the body; command carries PHOSPHOR_PORT=4216 and PHOSPHOR_DATA_DIR=<data dir> only (agents-catalog.ts:515-519, spec from cfg at mutation.ts:45-54). Host evil.com, 127.0.0.1.evil.com, [::1] -> 403; localhost -> 200; foreign or null Origin -> 200 with no Access-Control header anywhere in src/http (grep 0), the same shape as /api/state; router.ts:170 hostIsLocal runs before every route. ?agent=evil -> 400. agent-scan, agent-check, agent-pick without the token -> 403, seat-as-token 403, no agent.json written.
- Q3 (10.4, 10.e) PASS. Picks with the token against the real binaries and isolated homes: claude wrote ~scratch/.claude/.claude.json mcpServers.phosphor {command node, args [src/mcp.ts], env {PHOSPHOR_PORT, PHOSPHOR_DATA_DIR}}; codex ~scratch/.codex/config.toml [mcp_servers.phosphor] + env with both keys; hermes ~scratch/.hermes/config.yaml phosphor with both keys, enabled true; grok ~scratch/.grok/config.toml with both keys. Nothing outside the vendor homes plus <dataDir>/agent.json (find -newer; worktree `git status` clean). Real ~/.codex, ~/.hermes, ~/.grok: hash and mtime identical before and after. Real ~/.claude.json changed hash at 20:44:33, 8 s BEFORE my claude pick (scratch backup stamped 20:44:41): no phosphor entry, no 4216, no review-c-secure in it (grep 0), a running Claude Code session's own housekeeping, not the picker. Injection: the body contributes the agent id alone, validated by agentById (mutation.ts:303, catalog:176-179); argv is catalog constants plus connectionSpec (catalog:525-544); spawn(bin, args) with no shell (catalog:266); no vendor file is written by the app itself. Arguments a person could not have typed: only the prior `mcp remove` (catalog:563-574, 606) and "Y\n" on Hermes's stdin (catalog:605); the add line equals the Details line.
- Q4 (10.e) PASS, with the PATH point proved. Fake `codex` first on PATH of a backend whose env held PHOSPHOR_WINDOW_TOKEN, PHOSPHOR_SEAT and SECRET_CANARY (ps eww confirmed): agent-check ran it for `login status` and `--version`; env it saw: CODEX_HOME HOME LANG PATH SHELL TERM TMPDIR USER (+ sh's own _ PWD SHLVL); no PHOSPHOR_*, no token, no canary (catalog:248 PROBE_ENV, :301-308); stdin /dev/fd/0 is a character device, read gives EOF (catalog:266 'ignore'); the response carried the bin path and the 80-char version line only. Same privilege as the person typing `codex`, no elevation. Grok: auth.json is read and JSON-parsed for its key count only (catalog:363-371), nothing from it in the response or audit.jsonl; hermes: regex on `config get model` stdout, returns in/out (catalog:348-356).
- Q5 (1.11) PASS. inApp true for claude alone (catalog:82; :99, :116, :133, :150, :167 false); Start it only when check.inApp (firstrun.js:859-862) through the unchanged driver start; driver.ts diff touches resolveClaudeBin only, argv still --settings operator/driver.settings.json, --mcp-config, --strict-mcp-config (driver.ts:215, 422, 424); operator/ untouched; lockdown suite ran the installed claude (4.1 to 4.2 s each, not skipped): 4/4.
- Q6 (rule 4, rule 12, 9.4) PASS. Added lines: hex32+ 0, 0x addresses 0; test tokens are 'a'/'b'.repeat(64); ten fixtures carry no credential shape; gitleaks --log-opts=main...rfp/c-agent: 0 (the 185 whole-tree hits are pre-existing token-list files, none in the diff). All 17 PNGs opened: no token, seat or key; Details and the paste line show port and data dir path only. Terms: src/terms.ts, terms.js, terms.css untouched, #screen-terms hides #page while data-terms is set, terms.png taken first. Threshold: input starts at the policy's own 100 (firstrun.js:48, :901), presets 25/100/500, no skip on that step, fresh data dir policy.json thr 100 before any POST.
- Q7 (1.c) PASS. `npm test`: tests 3123, pass 3123, fail 0, exit 0 (the report's demo-rail failure is fixed on main by bb4b842 and merged). `node --test tests/injection.test.ts tests/lockdown.test.ts`: 33/33.

3. Frozen rules touched: 1 holds (no approval path added, tool set unchanged, Q1); 4 holds (Q2, Q6; window token still read off stdin, never served: router.ts GET table has no session route); 10 holds (no eval or judge edits, the four new suites test behaviour); 12 holds (terms first, threshold prefilled with the existing default, gates untouched); 7 (b) list untouched, injection suite green after the UX commits.

4. Findings
- A (Medium, one line): src-tauri/src/main.rs mcp_command reads GET /api/connection with backend::request_within and copies `command` verbatim to the clipboard, never calling identity_matches with this boot's nonce (backend.rs:362, used for phosphor_is_listening at :374). A process under another account or a sandboxed one that binds 127.0.0.1:<port> during the respawn backoff hands the person a command the app's own dialog tells them to paste. The security model built the nonce for exactly that race. Refuse unless identity_matches, and refuse a command that is not one printable line.
- B (Medium, product call): registration is user or global scope (catalog:532 claude --scope user, :534 codex global toml, :538 hermes global yaml, :540 grok --scope user). Phosphor's money tools are now in every session of that agent in every directory, so a prompt injection in any repo or page the agent reads elsewhere can propose under the threshold and it runs while the vault is open (policy allow, by design, security-model.md "contained for approval and not for proposal"). The old line was local scope. Neither the tile, Details nor docs/connect-an-agent.md says so. Say it in Details, or offer local scope.
- C: hermes registration starts the proxy against the live backend and answers "Enable all N tools" with Y (catalog:601-607); expected, but it is the one registration that opens an MCP session on the person's behalf.
- D: done.png says "Your assistant is connected" after a Claude Desktop pick, where nothing is connected (firstrun.js:948). Copy, node C or D.

5. Claims not reproduced
- 10.4 "PHOSPHOR_PORT and PHOSPHOR_DATA_DIR in every mode", cited to evidence-c/registration-live.md: that file shows codex's env block with PHOSPHOR_DATA_DIR only and grok's env block empty (lines 11-12, 21-22). The merged code writes both keys for both (my run above), so the evidence predates the fix or was trimmed. Re-capture it.
- 2. Counts "3122 pass, 1 fail": now 3123/3123 after the main merge; not a defect.
- 10.2 "presence of ~/.grok/auth.json (values never read)": the file's bytes are read and parsed (catalog:366); only the key count is used and nothing leaves. Wording, not behaviour.

Cleanup: both backends quit (4216, 4217 free), fake binary removed, real vendor configs untouched (hashes above). No browser opened, so nothing to close; `brave-automation done` left to the session-end hook because peers hold tabs on the shared browser.
