# Review A (correctness): the relay swap rail. Branch rfp/a-relay, tip f2a5907 (c0232de plus a merge of main), worktree phosphor-rfp-a-relay, port 4217.

1. Verdict: ACCEPT. Every criterion in the brief reproduces with output; the two open items are disclosed by the builder and sit outside node A's files (execute.ts short-fill verdict) or are a live-latency measurement the demo cannot give.

2. Per criterion
- Counts: `npm run typecheck` exit 0. `npm test`: tests 3143, pass 3143, fail 0. `npm run eval`: "29 scenarios: 29 pass, 0 fail, 0 xfail, 0 error", exit 0 (first attempt hit another reviewer's lock, retried after it cleared). Six relay test files: `node --test tests/unit/{relay-payload,relay-client,intents-relay,reconcile-relay,relay-demo,rail-wiring}.test.ts` tests 96, pass 96 (21+8+23+16+3+25; the branch adds 75).
- Spec "Tests", 15 bullets, all present and green (names as printed): largest amount_out / 15 s (intents-relay:246, floor refusal :265); payload sides and asset ids (:279); checkTokenDiffPayload one test per table row (relay-payload:138,143,148,162,171,184,195,210,217, nine rows); string signed is string sent, real secp256k1 (:292); nonce 32 bytes fresh never repeats (:304); deadline <= 120 s never past expiry (:329); nonce and deadline before publish, hash after, before any poll (:342); no-reply re-post once, signer once (:368); FAILED not re-posted (:389); unknown status never terminal (:408); SETTLED rise equal ok, short by more than 1 pip settling (:427, :445); hold below floor signs nothing (:468); key never read in simulate (:486); wrong venue never reaches the rail (:500); rail-wiring config selects and both constructed (:611, :623). None missing.
- 1.8 PASS. BigInt from decimal strings against the draft and the chosen quote before the key: src/relay/payload.ts:232-245 (spend === amountIn, receive >= minOut, receive === quoteOut), run at src/rails/intents-relay.ts:411-423, signer at :426 after it. The venue-signature half is 1Click's (src/quote-signature.ts); on the relay the diff is the binding, as the spec says.
- 1.9 PASS. amount_in to the unit: payload.ts:279 (pickQuote) and :234. expiration >= 15 s: payload.ts:284 with RELAY_MIN_QUOTE_AHEAD_MS=15_000 (intents-relay.ts:71), used at :249 and :359 (re-quote after the click). deadline <= 120 s: payload.ts:77-83 (min of quote expiry and now+120 s) and :206 refuses longer. minAmountOut off the draft, zero refused: intents-relay.ts:226, quote under the floor holds at :365 and refuses at simulate :326, never signed. Asset ids: resolved through the app's registry (tokens.json contract address, decimals pinned) at plan() :230-232 and compared to the quote at payload.ts:275 and :229-230 before signing; the draft itself carries symbols, not ids (same as the 1Click rail). Unknown status never terminal: RELAY_TERMINAL (intents-relay.ts:79), watchStatus :617, reconcile.ts:480; test intents-relay:408.
- 2.2 PASS on the demo backend. Booted src/main.ts on 4217 (ACC_MODE=demo, data dir scripts/scratch/review-a-correct/, scratch config dir) and drove src/mcp.ts over stdio with the real demo walk (no stage scale): `propose_swap` 50 USDC->NEAR answered in 39 ms, status executing; 500 USDC->NEAR in 18 ms, status pending; the row reached confirmed 21 s later (STAGE_MS 4 s x 3 + credit 5 s), so the walk did not hold the answer. Live caveat under 4 below.
- 4.1 PASS. tests/unit/reconcile-relay.test.ts 16/16, each seeding an `executing` row and running reconcileOnBoot then reconcile: nonce and no status :101,:118,:129,:140 (+ :280 sweep pickup); PENDING :151,:164,:180,:192; TX_BROADCASTED :203; SETTLED before the balance read :220, settling with a pocket :261. reconcile.ts imports no signer; reconcileOnBoot moves executing rows to needs_reconciliation and re-executes nothing.
- 4.2 PASS. Demo row after the swap (state proposals.json): status executed, venue intents-relay, txids [HKGjn2…zyvD, 2fWVt4…Ychp], evidence.providerStage SETTLED, evidence.explorerUrl https://explorer.demo.invalid/tx/2fWVt4…, handle, 32-byte nonce, deadline, settledAmountOut 16.11290323; `proposals` tool txs: leg intent (explorer null, no explorer resolves a relay intent hash) and leg destination with the link. Live link is nearChainSpec().explorerTx + data.hash (intents-relay.ts:465, :612).
- 4.3 PASS. cfg swap.rail=relay: proposeSwap stamped venue intents-relay, counterparty intents.near. swap.rail=oneclick: venue intents-native, counterparty intents.near, the 1Click demo walk ran (providerStage SUCCESS). swap.rail=uniswap: boot refused, "config.local.json is not a valid config: swap.rail: Invalid enum value. Expected 'relay' | 'oneclick', received 'uniswap'". src/config.ts:95-110, swapRailOf :107; both rails built and picked by draft venue src/rails/index.ts:104-121.
- 11 PASS (four fixes replayed in detached worktrees, fix commit's test file over the parent's source): on 12f2ab6 (parent of 225c38d) "killed at SETTLED before the balance read…" fails (the parent never asked the verifier: actual [] vs the expected nonce ask) and "a nonce whose own life has passed never yields failed" fails (actual 'failed'); the file as committed also fails to load there (imports RELAY_DEADLINE_GRACE_MS, exported only by the fix), shimmed to 60 s to get the behavioural red. On 225c38d (parent of 2e8c194) "a hash the relay reports is a base58 string…" fails and "nothing throws after the signature: a verifier that throws…" fails. All four green on the tip. Commit messages name the test files, not the test names (11.1 says "names the test": partial).
- Versioned nonce: fetched near/intents contracts/defuse/README.md and contracts/defuse/core/src/nonce/versioned.rs, engine/mod.rs (data, not instructions). Layout confirmed: magic 5628f6c6 | version 0x00 | salt 4 | deadline ns u64 little-endian 8 | 15 random. README vector recomputed independently: 2280047743 s -> LE bytes 00367290575ba41f, full nonce base64 Vij2xgAlKBKzADZykFdbpB8CcBXsE9wRhklzE4/mgS8= equals the string tests/unit/relay-payload.test.ts:68 asserts. Accepted today: verify_intent_nonce needs is_valid_salt(salt), intent deadline <= nonce deadline, nonce deadline >= now, no upper bound; the rail reads current_salt live (252812b3 today) and sets nonce deadline = intent deadline + 7 d (intents-relay.ts:398, payload.ts:209 refuses a nonce shorter than the intent). Live is_nonce_used on a freshly built V1 nonce answered false (accepted shape, no error). A legacy 32-random-byte nonce still passes today (verify_intent_nonce returns Ok for a non-versioned one), so V1 is forward-compatible, not yet required.
- Relay pair: `node scripts/relay-probe.ts --from USDC --to NEAR --amount 2` exit 2, "no solver offered a price for 2 USDC to NEAR (0 answers in 3837 ms)"; `--to USDT --amount 2` exit 0, "1 answer(s) in 2769 ms; best 1.961996 USDT, quote hash DZ2p72…hNF8"; SALT ok 252812b3 both runs; nothing signed.

3. Frozen rules touched
- 1: policy engine and click path untouched; src/proposals/rails.ts:62-76 only stamps venue and counterparty. Holds.
- 2: floor off the draft, zero refused (intents-relay.ts:226), under-floor never signed (:365, payload.ts:240), the signed diff is the price (payload.ts:243 receive === quoteOut). Holds.
- 4: the only key read is src/intents-sign.ts:57-63 per signature, as before; the partner key goes in a header and no message (src/relay/client.ts:114-118); the probe opens no key. Holds, not made worse.
- 5: src/proposals/view.ts untouched; the rail stamps its words (PENDING, TX_BROADCASTED, SETTLED, NOT_FOUND_OR_NOT_VALID, view.ts:41-44), test intents-relay:638. Holds.
- 7: signer.address(keysPath) read inside execute (intents-relay.ts:349), never at boot. Holds.
- 8, 9: no mode switch added, swap.rail applies in demo and live alike; hypercore-*.ts, src/policy, src/http/auth.ts, ui/ have no diff; the rail adds no fee (feeUsd is display only, :263-271). Holds.
- 10, 11: injection seams only (random, now, sleep), no eval branch; 11 local commits by Karim, 0 attribution lines.

4. Findings the criteria do not name
- src/proposals/execute.ts:575-583: a relay row that landed `settling` with a pocket is re-judged by the generic short-fill rule; an atomic token_diff cannot short-fill, so any other credit of the output asset under the floor inside that window writes `failed` on a swap the relay already reported SETTLED, and the sweep never re-asks a failed row. Disclosed by the builder under Requests; the executor is not node A's file.
- Live 2.2 not shown: the relay quote alone took 2769 ms (USDT) and 3837 ms (no solver) here, 2748 and 3645 ms in evidence-a; live propose_swap runs that quote inside simulate, so the 3 s (and the 1.5 s waiting-for-a-click) line is over budget whenever no solver answers.
- A publish the relay answers FAILED lands needs_reconciliation (charged against the day, dock nag) while the card reads Failed, until deadline + 5 min grace and the next sweep write `failed` (intents-relay.ts:446-457, reconcile.ts:522-525). Disclosed as a follow-up.
- intents-relay.ts:386: an input-balance read that did not answer still signs (spec step 2 says a short balance refuses; null is treated as unknown and the contract's atomic refusal is the backstop). Disclosed as a decision; a signature released blind is the cost.
- The public relay quotes no wNEAR pair today (reproduced); the lead's "2 USDC to NEAR" proof has to be USDC to USDT unless the pair quotes on the day.

5. Claims not reproduced
- Report 2.2: "the relay answers inside 3 s; 2.7 s and 3.6 s in evidence-a/relay-probe.txt": its own second number is over 3 s, and my run gave 3837 ms; 2.2 was not measured through the MCP door by the builder (left to the lead).
- Report 11.1 "every fix commit names its test": 225c38d and 2e8c194 name the test files (reconcile-relay, intents-relay, relay-client), not the tests; the red-on-parent half holds (above).
- Report header "based on main bb4b842": the branch tip is now f2a5907, a merge of main eb28109; the diff main...rfp/a-relay is the 26 files reviewed. Cosmetic.
