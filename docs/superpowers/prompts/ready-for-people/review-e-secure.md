# Review E, secure: UI craft floor and buttons (rfp/e-craft, 3da97fd..ed0f0e4)

1. Verdict: ACCEPT on security. No gate is removed, delayed, hidden, auto-answered or pre-filled; every frozen rule the diff touches still holds; injection 29/29 and lockdown 4/4 green. One B6 miss for the lead to route (section 4, first line): it is main's structure, not a weakening by this diff.

2. Per question
- Q1 gates, PASS. Diff touches only ui/design/*.css, ui/index.html:92, .impeccable/config.json, scripts/button-inventory*, tests/unit (`git diff --stat main...rfp/e-craft -- src/ src-tauri/ ui/screens/ ui/core/ tests/injection.test.ts tests/lockdown.test.ts` is empty). The dock is a scrolling `.dock-body` plus a `.dock-foot` that holds only the error note (decision.js:1242-1243, say() 1270); No/Yes live in the body AFTER the facts by main's design (decision.js:588-598 "THE ANSWER COMES AFTER THE FACTS", `var foot = body`), and CSS adds no `order`/`reverse` (grep of the diff: none). Rendered on my demo backend (4212, swap 300 USDC and send 250 USDC parked, `scripts/scratch/review-e-secure/dock-check.ts`): factsBeforeActions=true and addressBeforeActions=true in all 9 viewports; full address on the send card `0x8ba1f109551bD432803012645Ac136ddd64DBA72` (42 chars, groups of 4); fee and floor lines before the click: "Arrives at least 242.5 USDC", "Fee $0.35 (0.1 USDC bridge)"; Yes disabled during REARM_MS 600 and awaiting_touch (decision.js:679-712, untouched). Freeze everything in view at every size (`freezeInView: true`, 30 px box, index.html:92 `btn btn-sm freeze`, label unchanged, data-pending-label matches shell.js:366). Terms, lock, deposit double check: terms.css, lock.css, firstrun.css, vault.css untouched; deposit.css adds only `.netpick-link` sizing (netpick.js:778, 1356). Stacked rule layout.css:537-541 collapses the world row under 960 px while an ask is up; the topbar with Freeze is outside `.stage` (index.html:92 vs 103), so the kill switch stays.
- Q2 rule 12 and 300 ms, PASS. Timing lives in JS and tokens.css, both untouched: `--dur-swap` transition on `.btn-pending` (components.css:229-235) unchanged; the two-face fix only puts `button[data-pending="true"] > *` in the one grid cell (components.css:191-200). agent.js:1188 STARTING_FLOOR_MS = 450 untouched. Note: the dock's decide() (decision.js:1150-1185) has no floor on main either, a rejected /api/approve paints the error in the same frame; pre-existing, not E's.
- Q3 display:none, PASS. Three new rules only: `.composer-send .btn-pending-label` (agent.css:1151, the send disc's verb, spinner stays), `.chip > .dot` (components.css:542; only vault.js:96 chips carry one: custody, backup, Verified/Unverified; the word and data-tone colour remain; the bar's "2 waiting" / "No backup" dots are `.bar-state`, not chips), `.split-v` in the stacked ask (layout.css:540-542). `.dock-note` uses the hidden attribute with reset.css:91 `[hidden]{display:none !important}` untouched; `.dock-foot:empty{display:none}` was on main (main:layout.css:626). Queue line renders: "1 more waiting, next" at the top of the body in every capture.
- Q4 detector ignores, PASS. Baseline (impeccable-detect-baseline.json) had exactly "html clips a positioned child", "body clips a positioned child" (layout.css:9-12 by design) and the hairline+shadow advisory; no component-level clip of a menu or sheet was in the set. Residual: `value: "*"` scoped to ui/index.html silences any FUTURE clipped-overflow finding the detector attributes to the document (it reports DOM findings at index.html:0), so a real clipped menu later would be muted. Advisory, not a finding today.
- Q5 index.html, PASS. One line changed (index.html:92: `btn-sm`, `type="button"`, `data-pending-label="Freezing"`). No script, style, font or CDN added (grep `url(|@import|http|<script|on[a-z]+=` over the ui diff: none); the two http strings in the file are w3.org SVG namespaces. CSP is a header from src/http/respond.ts:189 (untouched); tauri.conf.json untouched; fonts still ./fonts/Sora and GeistMono (index.html:10-11); window token handling in ui/core untouched.
- Q6 inventory script, PASS with two notes. No backend, no port, no ~/.phosphor, no keystore: it renders a generated sheet over the stylesheets on disk with Playwright's own headless Chromium (button-inventory.ts:229-230), never the daily Brave. Writes: INVENTORY_OUT (default the evidence folder) and an os.tmpdir() sheet dir (button-inventory.ts:237-239) that is never removed (one `phosphor-button-inventory-*` dir with two 19 KB HTML sheets left in $TMPDIR). Prints the table only; grep for 40-hex, 64-hex, token=, Bearer over inventory.json/md, recipes.ts: none. Note: it `require`s playwright-core from an unpinned npx cache path (`~/.npm/_npx/705bc6b22212b352/...`, button-inventory.ts:31), code outside the repo and the lockfile; same pattern as checks-proof.ts:23, so not new, but the script fails on any other Mac.
- Q7 tests, PASS. `npm test`: "tests 3096, pass 3096, fail 0" (scripts/scratch/review-e-secure-npm-test.log). `node --test tests/injection.test.ts`: tests 29, pass 29, fail 0. `node --test tests/lockdown.test.ts`: tests 4, pass 4, fail 0. Both suites byte-identical to main.

3. Frozen rules touched: 12 only (a UX reshape of the dock and buttons). Holds: the dock still asks (layout.css:690-704 `.dock`, decision.js untouched), the address is untruncated (rule 3, sendcard.css:129 `overflow-wrap: anywhere` kept, rendered 42 chars), nothing pre-fills or auto-answers (no JS in the diff). Rules 1-11: no file they live in changed. Rule 4: my run printed a throwaway demo mnemonic into my scratch log (wallet/create returns it by design); scrubbed, demo keys deleted, none of it is in this report.

4. Findings the criteria do not name
- B6 is met for the swap card only. The send card (the gate that matters most) keeps No/Approve under the fold at every desktop size: bottom 928 px in a 780 px window at the app default 1180x780 (default column), 909 in 800 at 1280x800, 909 at 960x700; on screen only at 1440x900. The dock shows the fade (data-more=true) and scrolls, facts first, so the gate is intact; but the report's "B6 PASS" was measured on a swap only (report-e.md section 3, commit 14a10d3). Captures: scripts/scratch/review-e-secure/dock-1180x780-default-card2.png. Route to the correctness reviewer or node D (decision.js owns the body/foot split).
- At 400x700 (browser only, under the 960x700 desktop floor) even the swap card's No/Yes sit 34 px under the fold (bottom 726 of 700).
- `.dock{flex:0 0 auto}` plus `.composer-host{flex:0 0 auto}`: with a six-line draft in the composer (COMPOSER_MAX_LINES 6, agent.js:134) and an ask up, the composer's bottom is clipped by `.conversation{overflow:hidden}` (layout.css:362); before the diff the dock shrank instead. Not a gate; the trade favours the answer.
- `.sendcard-address-actions .icon{14px}` was dropped (sendcard.css:193-205) but only `.sendcard-copy > .icon` got it back (components.css:120-124); the Explorer link's external glyph (sendcard.js:372) now sizes at 1em. Cosmetic.
- The inventory's temp sheets are left in $TMPDIR each run (button-inventory.ts:237).

5. Claims I could not reproduce
- "B6 PASS ... every button in the dock is visible without scrolling at 400 px": true for the swap card (400x800: No/Yes bottom 726 of 800), false for the send card at every size I ran (section 4). Their evidence never shows a send card.
- Everything else in report-e.md that my brief covers reproduced (counts, the freeze class, the ignores, the three display:none rules).

Cleanup: backend on 4212 killed (nothing listening), MCP child closed, my browser contexts closed (no 4212 tab left), `brave-automation done` left the browser up for two peer sessions' tabs (4211, 4215), demo keys and data dir deleted, worktree clean.
