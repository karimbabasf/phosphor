# Button inventory: verdict per family

The table with the sites, label source, verbs and states drawn is `inventory.md` (generated by
`node scripts/button-inventory.ts`, 108 sites, 54 families, 108 measurements, 0 problems after
the fixes). The sheets are `sheet-860.png` and `sheet-400.png`: every family in rest, hover,
active, focus, disabled, pending, on and live, forced through the inspector protocol, in the
shipped colourway. This file says what the sheet found and what changed, one line per family.

| Family | Verdict |
| --- | --- |
| `btn btn-primary` | fixed: hover put a grey rim on the green fill; hover and press now step the fill toward white (the label is the ground, so a step toward the ground went under 4.5:1 on the dimmest legal accent) |
| `btn btn-primary btn-lg` | ok (44 px, the pending face reserves its width) |
| `btn btn-primary btn-sm` | ok (30 px) |
| `btn btn-primary lock-unlock` | ok |
| `btn btn-ghost` | fixed: the press now steps the fill (was scale alone, invisible on a still frame) |
| `btn btn-ghost btn-lg` | ok |
| `btn btn-ghost btn-sm` | ok |
| `btn btn-ghost btn-sm agent-connect-back` | ok |
| `btn btn-ghost btn-sm tcard-open` | fixed: the hook had no rule; the button now sits on the card's text column (cards.css) |
| `btn btn-ghost btn-sm trade-more` | fixed: 28 px, under the small variant's 30; the override is gone |
| `btn btn-ghost activity-more` | ok |
| `btn btn-ghost trade-act` | fixed: 26 px on a .btn; the override is gone, it holds 36 |
| `btn btn-quiet` | ok (label on the second tone, 5.0:1 on the lightest legal ground) |
| `btn btn-quiet btn-sm` | ok |
| `btn btn-quiet btn-sm receipt-copy` | fixed: the copy button is drawn once for both copies (components.css) |
| `btn btn-quiet btn-sm sendcard-copy` | fixed: the hook had no rule; it shares the copy button's rule |
| `btn btn-danger` | fixed: hover edge in its own red, not grey; the label depends on the down slot's 3:1 floor (request filed) |
| `btn` | ok |
| `btn btn-sm freeze` | fixed: 28 px, under the .btn floor; small variant by class in index.html, names its verb (Freezing) in the markup |
| `chip` | fixed: no pressed face on aria-pressed, 26 px; pressed chips sit in the ink wash and hold 30 |
| `chip agent-retry` | fixed: 26 to 30 (button.chip) |
| `chip chip-filter` | fixed: 28 to 30, its own pressed rule folded into the shared one |
| `chip connection-copy` | fixed: 26 to 30 |
| `chip suggest` | ok (32) |
| `tab` | fixed: 28 to 30, and a hover mark (a tab goes somewhere) |
| `layout opens` | fixed: 28 to 30 |
| `status-line bar-state` (the Not backed up button) | fixed: a button that looked like a static line; 30 px box, pointer, hover lift |
| `composer-send` | fixed: a stop in flight put the spinner and the word Working beside the glyph in a 36 px disc; the faces stack and the disc waits with the spinner alone |
| `steps-fold` | fixed: 22 to 30, label off the third text tone |
| `fold-head card-head` | ok (hover lifts the meta and the mark) |
| `checks-toggle` | fixed: no hover; the summary and chevron lift |
| `dock-next` | fixed: 28 to 30 |
| `dock-report-toggle` | fixed: no hover; the count and chevron lift |
| `dock-close` | fixed: 24 to 30 (16 px glyph) |
| `choice` | ok |
| `lock-eye` | ok |
| `net-tile` | ok |
| `netpick-link` | fixed: 21 px text link; 30 px box with 12 px of room each side |
| `net-row` | ok |
| `netpick-back` | ok |
| `netsel` | ok |
| `holding-head` | ok (opens only when it has places under it) |
| `rule-group-title opens` | fixed: 21 to 30, label off the third text tone, hover to the first |
| `receipt-close` | ok (32) |
| `tx receipt-row` | ok (a row whose title is a sentence; it wraps on a narrow column by design) |
| `activity-link` | fixed: 29 to 30, 12 px of room each side, never wraps |
| `sendcard-info` | fixed: 16 px disc; now 30 px to press, 16 px to read, the focus ring on the disc |
| `trade-tab` | ok (35) |
| `layers opens` | fixed: 26 to 30 |
| `trade-symbol opens` | ok |
| `check-row layers-row` | fixed: the hook had no rule and the only copy lived in trade.css with a round box that read as a radio; one component in components.css, 30 px, square box, hover wash |
| `pane-hide` | fixed: 24 to 30 |
| `pane-show` | fixed: 24 to 30 |
| `jump-latest` | ok (32) |

Not a button family but on the same sheet: the vault's status chips carried a dot beside the word
(a status pill with a dot); the dot is gone (`.chip > .dot`).
