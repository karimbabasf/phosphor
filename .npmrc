# No dependency runs code at install time, here, in CI or in the release bundle. Nothing in the
# lock carries an install script today; this is for the version of one of them that does.
ignore-scripts=true
